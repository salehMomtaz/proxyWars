# XHTTP: فراتر از REALITY

## خلاصه توسعه
در اواسط سال ۲۰۲۴، افراد زیر بر اساس اصل **«ارسال بسته‌ای بالا‌رونده، جریان پایین‌رونده»** که توسط @RPRX توضیح داده شد، SplitHTTP را توسعه دادند:
- @mmmray
- @ll11l1lIllIl1lll

این پروژه **برای اولین بار توانست بدون قربانی کردن کارایی پایین‌رونده، از اکثر میدباکس‌های پشتیبان HTTP عبور کند و همچنین اولین پیاده‌سازی گسترده QUIC H3 از طریق CDN را محقق سازد** و عصر جدیدی را آغاز کند. پشتیبانی از **مرورگر به عنوان دیالر (Browser Dialer)**، **پر کردن هدر برای کاهش ویژگی‌های شناسایی (header padding)**، **XMUX برای کنترل مالتی پلکسینگ** و **فعال‌سازی REALITY** به‌تدریج اضافه شدند.

سپس @RPRX توسعه را بر عهده گرفت و **جداسازی واقعی بالا‌رونده و پایین‌رونده را پیاده‌سازی کرد و نام آن را به XHTTP تغییر داد**. به‌عنوان مثال، بالا‌رونده و پایین‌رونده می‌توانند به‌ترتیب **IPv6 CDN H3** و **IPv4 REALITY H2** باشند (حتی IP مبدأ نیز می‌تواند متفاوت باشد). ~~این بار نیز عصر جدید دیگری آغاز شد.~~

در ادامه، **حالت ارسال جریان‌ای بالا‌رونده (stream-up)** که کارایی بالا‌رونده را قربانی نمی‌کند، و **طرح extra** که امکان اشتراک‌گذاری تمام جزئیات پیکربندی را فراهم می‌کند، توسعه داده شد. همچنین برای حالت stream-up، **تقلید هدر gRPC به‌صورت پیش‌فرض** اضافه شد که **امکان عبور جریان‌ای بالا‌رونده H2 از CDN را فراهم کرد و جایگزین لایه انتقال سنتی gRPC شد**. ~~اگر از ابتدا می‌دانستیم این امکان وجود دارد، شاید gRPC را توسعه نمی‌دادیم.~~

در نهایت، **لایه انتقال HTTP به‌عنوان حالت stream-one در XHTTP ادغام شد** و ویژگی‌هایی مانند header padding، XMUX و تقلید هدر gRPC را نیز به‌دست آورد.

**بدین ترتیب، XHTTP به شکل کامل خود رسید: عبور از میدباکس‌ها به روش‌های مختلف، جداسازی بالا‌رونده و پایین‌رونده، XMUX روان و هر آنچه نیاز دارید. عصر همه‌کاره‌بودن XHTTP رسماً آغاز شده است.**

این متن در ابتدا به‌عنوان یک راهنمای ویرایش‌شده در نظر گرفته شده بود، اما ناخواسته به مقاله تبدیل شد. هدف این مقاله کمک به درک عمیق اصول و طراحی XHTTP برای استفاده بهتر از آن است.

## اگر قصد حمایت مالی از پروژه Xray یا خرید NFT را دارید، توضیحات مربوطه در انتهای مقاله آمده است. از حمایت شما سپاسگزاریم!

## راهنمای سریع شروع

با وجود پارامترهای نسبتاً زیاد XHTTP، همه آنها به‌صورت پیش‌فرض تنظیم شده‌اند. اگر فقط قصد استفاده از XHTTP را دارید، کافی است شش مرحله زیر را دنبال کنید:

۱.  چه برای TLS و چه برای REALITY، **به‌طور کلی برای پیکربندی XHTTP فقط کافی است `path` را پر کنید و بقیه موارد را خالی بگذارید.**
۲.  اگر سرور از QUIC H3 پشتیبانی می‌کند، **در سمت کلاینت کافی است `alpn` را روی "h3" تنظیم کنید تا از QUIC استفاده شود.**
۳.  برای استفاده از IP بهینه‌شده CDN، در سمت کلاینت `address` را با IP پر کنید و `serverName` (SNI) را با نام دامنه پر کنید.
۴.  اگر به Cloudflare متصل نمی‌شوید، **پشتیبانی gRPC را در پنل Cloudflare فعال کنید.**
۵.  اگر از Nginx عبور نمی‌کند، **دستور `proxy_pass` در Nginx را به `grpc_pass` تغییر دهید.**
۶.  اگر از سایر CDNها یا نرم‌افزارهای پروکسی معکوس عبور نمی‌کند، توصیه می‌شود `mode` را روی "packet-up" تنظیم کنید که بیشترین سازگاری را دارد.

**XHTTP به‌صورت پیش‌فرض از مالتی پلکسینگ (چندمسیره) پشتیبانی می‌کند. تاخیر آن کمتر از Vision است، اما در تست سرعت چندنخی به پای Vision نمی‌رسد، مگر اینکه قبل از تست `"maxConcurrency": 1` تنظیم شده باشد. به بخش XMUX مراجعه کنید.**

Cloudflare اتصالات HTTP که در پایین‌رونده به مدت ۱۰۰ ثانیه فاقد داده واقعی باشند را قطع می‌کند. برای حفظ اتصالات بلندمدت پروکسی، باید در لایه کاربردی اقداماتی مانند تنظیم `ClientAliveInterval` در sshd برای SSH انجام داد.

می‌توان از این مقاله به‌عنوان یک مستند پیشرفته استفاده کرد. این مقاله تقریباً تمام مواردی که می‌خواهید درباره XHTTP بدانید را پوشش می‌دهد و هر پارامتر را توضیح می‌دهد. **در انتهای مقاله نیز یک نمونه پیکربندی شامل تمام پارامترها به همراه توضیح کاربرد هر یک ارائه شده است. اگر درباره پارامتری سوال دارید، کافی است نام آن را در متن مقاله جستجو کنید.**

## دو منطق پایه‌ای

همانطور که [REALITY](https://github.com/XTLS/REALITY) می‌تواند خود را به‌عنوان وب‌سایت دیگری جا بزند، **منطق بنیادی مقابله با سانسور، افزایش «خسارت جانبی» برای سانسورچی در هنگام اجرای блок است تا او از блок کردن سریع و بی‌محابا خودداری کند.** سال‌ها پیش نیز به همین دلیل به TLS و پنهان‌سازی الگوهای زمانی و طولی ترافیک روی TLS خوش‌بین بودم. تجربه سال‌ها به ما نشان داده که دیوار آتش بزرگ به‌طور دائمی کل IPهای یک CDN بزرگ را مسدود نمی‌کند، زیرا این کار ترافیک بسیاری از وب‌سایت‌های معمولی را مختل می‌کند. **بنابراین هدف اولیه ما برای XHTTP این بود که آن را پشت CDNهای متنوع و متعدد پنهان کنیم.**

با این حال، CDNها برای محافظت از سرورهای مبدأ در برابر حملات، معمولاً کل درخواست HTTP را دریافت و کش می‌کنند و سپس آن را به مبدأ ارسال می‌کنند (به‌جز موارد خاصی مانند WebSocket و gRPC که پشتیبانی ویژه دارند). بسیاری از میدباکس‌های HTTP نیز به‌صورت پیش‌فرض همین رفتار را دارند. بر این اساس، پروتکل Meek در Tor ترافیک رفت و برگشت را در قالب درخواست‌های HTTP مجزا بسته‌بندی می‌کند تا از این میدباکس‌ها عبور کند، اما سرعت آن به‌شدت پایین است زیرا از «جریان پایین‌رونده» XHTTP استفاده نمی‌کند.

**«جریان پایین‌رونده» چیست؟** تصور کنید در حال دانلود یک فایل بزرگ از یک وب‌سایت هستید. CDN کش آن را ندارد، بنابراین به سرور مبدأ مراجعه می‌کند. اما واضح است که CDN نمی‌تواند مانند بالا‌رونده، کل فایل را ابتدا دریافت و کش کند و شما را معطل نگه دارد. بلکه به‌محض دریافت داده از مبدأ، آن را بلافاصله به شما منتقل می‌کند. این اساس **«جریان پایین‌رونده» XHTTP است و مهم‌تر از همه، تضمین می‌کند که سرعت حیاتی پایین‌رونده به حداکثر برسد.**

در مورد بالا‌رونده، به دلایل سازگاری، XHTTP در ابتدا **«ارسال بسته‌ای بالا‌رونده»** را پیاده‌سازی کرد که در آن ترافیک بالا‌رونده در قالب درخواست‌های POST مجزا بسته‌بندی می‌شود. بدیهی است که کارایی این روش کاهش می‌یابد، اما خوشبختانه برای نیازهای معمول پروکسی، حجم ترافیک بالا‌رونده بسیار کم است. **پس از آن، من «جریان بالا‌رونده» را اضافه کردم و متوجه شدیم که با افزودن تقلید هدر gRPC، حتی می‌توان جریان بالا‌رونده H2 را نیز از Cloudflare عبور داد.** در ضمن، پس از چندین دور بهینه‌سازی «ارسال بسته‌ای بالا‌رونده»، سرعت آن حتی به سرعت «جریان بالا‌رونده» نزدیک شد.

در اینجا بد نیست به موضوع داغ اخیر یعنی «سوءاستفاده از CDN» نیز اشاره کنم: واضح است که CDNهایی که از جریان بالا‌رونده پشتیبانی نمی‌کنند، قصد نداشته‌اند از آنها برای راه‌اندازی سرویس پروکسی استفاده شود. اما ما برای مقابله با دیوار آتش بزرگ، ناگزیریم که به کاوش، توسعه و استفاده از راه‌های جدید تا حد امکان ادامه دهیم. افزایش «خسارت جانبی» برای سانسورچی مستلزم آن است که خود را در میان سرویس‌های «عادی» پنهان کنیم، و این امر اجتناب‌ناپذیر است. **ساده‌ترین مثال: اگر روزی لیست سفید IP اعلام شود و IPهای یک CDN در آن لیست باشند، آیا از آن استفاده می‌کنید یا نه؟** در حوزه مقابله با سانسور، برخی قواعد دنیای واقعی قابل اعمال نیستند. این منطق ساده هنوز برای برخی نامفهوم مانده است. اگر هنوز متقاعد نشده‌اید، می‌توانید لایه انتقال WebSocket را که امروزه صرفاً برای استفاده از CDN وجود دارد، به‌عنوان توسعه‌دهنده حذف کنید یا به‌عنوان کاربر پیشنهاد حذف آن را به توسعه‌دهندگان بدهید. با این اقدام عملی ثابت کنید که بحث «سوءاستفاده از CDN» صرفاً بهانه‌ای دیگر برای حملات روزمره و بی‌حاصل به Xray است که ناشی از حسادت و برخی تمایلات بیمارگونه است. ~~در عین حال، عملشان خلاف ادعایشان است.~~ در عین حال، به‌راحتی می‌توان ذات ریاکارانه برخی افراد را مشاهده کرد.

## PACKET-UP

**این بخش دارای جزئیات فنی زیادی است. کاربران غیرتوسعه‌دهنده می‌توانند فقط قسمت‌های پررنگ را بخوانند.**

**برای حالت «ارسال بسته‌ای بالا‌رونده، جریان پایین‌رونده» که بیشترین سازگاری را در XHTTP دارد (حالت packet-up)، طراحی به این صورت است:**

۱.  **کلاینت با ارسال `POST /yourpath/sameUUID/seq` داده‌های بالا‌رونده را ارسال می‌کند:**
    *   UUID به‌صورت تصادفی تولید می‌شود. UUIDای که بعداً برای شروع پایین‌رونده استفاده می‌شود باید با آن یکسان باشد تا سرور بتواند ارتباط برقرار کند. اگر سرور نتواند در مدت ۳۰ ثانیه ارتباط را برقرار کند، نشست خاتمه می‌یابد.
    *   seq از ۰ شروع می‌شود. باید body درخواست POST قبلی کاملاً ارسال شود (اما نیازی به دریافت پاسخ آن نیست) سپس درخواست بعدی ارسال شود.
    *   ممکن است چندین درخواست POST با احتمال کمی به ترتیب اشتباه به سرور برسند. سرور بر اساس seq آنها را مرتب می‌کند. به‌صورت پیش‌فرض حداکثر ۳۰ درخواست در حافظه پنهان نگه داشته می‌شود و در صورت превыن از این حد، اتصال قطع می‌شود.
    *   توجه: UUID و seq هر دو در مسیر (path) طراحی شده‌اند، نه در رشته پرس‌وجو (query string)، تا از بروز مشکلات عجیب جلوگیری شود.

۲.  **کلاینت با ارسال `GET /yourpath/sameUUID` پایین‌رونده را آغاز می‌کند. هدرهای پاسخ سرور شامل موارد زیر هستند:**
    *   `X-Accel-Buffering: no` برای اطلاع به میدباکس‌ها جهت غیرفعال کردن بافرینگ.
    *   `Cache-Control: no-store` برای اطلاع به میدباکس‌ها جهت عدم نیاز به کش.
    *   `Content-Type: text/event-stream` برای تقلید از server-sent events (سازگاری بهتر است. می‌توان با تنظیم `noSSEHeader` آن را غیرفعال کرد).
    *   اگر از HTTP/1.1 استفاده می‌شود، باید `Transfer-Encoding: chunked` نیز وجود داشته باشد. برای H2/H3 این مورد نیاز نیست.

۳.  برای جلوگیری از محدودیت‌های CORS در هنگام استفاده از **مرورگر به عنوان دیالر (Browser Dialer)**، سرور برای تمام پاسخ‌های GET و POST هدرهای زیر را شامل می‌شود:
    *   `Access-Control-Allow-Origin: *`
    *   `Access-Control-Allow-Methods: GET, POST`

۴.  **برای حل مشکل ویژگی‌های طول ثابت هدرهای درخواست و پاسخ HTTP:**
    *   هدرهای درخواست ارسالی از سمت کلاینت به‌صورت پیش‌فرض شامل `Referer: ...?x_padding=XXX...` هستند (قرار دادن در Referer برای جلوگیری از ایجاد درخواست‌های OPTIONS غیرضروری توسط Browser Dialer است). طول پیش‌فرض آن بین ۱۰۰ تا ۱۰۰۰ کاراکتر است که به‌صورت تصادفی برای هر درخواست انتخاب می‌شود. سرور به‌صورت پیش‌فرض بررسی می‌کند که آیا این مقدار در محدوده مجاز تعریف‌شده در سرور قرار دارد یا خیر.
    *   هدرهای پاسخ ارسالی از سمت سرور به‌صورت پیش‌فرض شامل `X-Padding: XXX...` هستند. طول پیش‌فرض آن بین ۱۰۰ تا ۱۰۰۰ کاراکتر است که به‌صورت تصادفی برای هر پاسخ انتخاب می‌شود.
    *   **این همان **header padding** است که من بارها به آن اشاره کرده‌ام. تنظیم مربوطه `xPaddingBytes` نام دارد.**
    *   پیشنهاد انتقال padding هدر درخواست به `Referer` توسط @rPDmYQ مطرح شد. انتخاب `XXX` نیز توسط او بود. `X` در Huffman encoding معادل ۸ بیت است.

**ارسال بسته‌ای بالا‌رونده و `Referer: ...?x_padding=XXX...` می‌تواند لاگ‌های نسبتاً زیاد و طولانی تولید کند. می‌توانید در نرم‌افزار پروکسی معکوس خود تنظیماتی اعمال کنید تا آنها را ثبت نکند.**

علاوه بر این، مانند سایر لایه‌های انتقال Xray، سرور هدر `X-Forwarded-For` را نیز می‌پذیرد تا IP واقعی کلاینت را به‌دست آورد. همچنین بر اساس `host` تعریف‌شده در سرور، host ارسالی از سمت کلاینت بررسی می‌شود (پیشنهاد شخصی من این است که اگر ضرورتی ندارد، آن را تنظیم نکنید، زیرا از قبل در مسیر (path) پنهان شده است).

**مراحل فوق ساده‌شده و ضروری حالت packet-up هستند. اما هنوز یک سوال کوچک باقی است: چگونه به‌طور دقیق درخواست‌های POST را مدیریت و محدود کنیم؟ سه پارامتر اختصاصی برای این کار وجود دارد:**

*   `scMaxEachPostBytes`: حداکثر حجم داده‌ای که کلاینت می‌تواند در هر درخواست POST حمل کند. مقدار پیش‌فرض ۱۰۰۰۰۰۰ (۱ مگابایت) است. این مقدار باید کمتر از حداکثر مجاز CDN یا سایر میدباکس‌های HTTP باشد. سرور نیز درخواست‌های POST بزرگ‌تر از این مقدار را رد می‌کند.
*   `scMinPostsIntervalMs`: فقط برای کلاینت. بر اساس هر درخواست پروکسی مجزا، حداقل فاصله زمانی بین ارسال درخواست‌های POST توسط کلاینت. مقدار پیش‌فرض ۳۰ میلی‌ثانیه است.
*   `scMaxBufferedPosts`: فقط برای سرور. بر اساس هر درخواست پروکسی مجزا، حداکثر تعداد درخواست‌های POSTی که سرور می‌تواند در حافظه پنهان نگه دارد. مقدار پیش‌فرض ۳۰ عدد است. در صورت تجاوز، اتصال قطع می‌شود.

**«بر اساس هر درخواست پروکسی مجزا» به این معنی است که هر اتصال پروکسی به‌طور مستقل شمارش می‌شود و بر دیگری تاثیری ندارد.** حتی اگر آنها درون یک اتصال H2 یا H3 مشترک باشند. این معنی **sc** یا همان **sub-connection** است. **برای کاهش اثرانگشت، دو پارامتر اول می‌توانند به‌صورت یک بازه تعریف شوند.** مثلاً به‌ترتیب به صورت رشته‌ای مانند "500000-1000000" و "10-50" که هر بار به‌صورت تصادفی از بازه انتخاب می‌شود. همه این پارامترها می‌توانند از طریق `extra` به کلاینت ارسال شوند (در انتهای مقاله توضیح داده شده است). **نکته قابل توجه دیگر این است که آخرین نسخه Xray، حالت packet-up را بهینه کرده و سرعت آن حتی به سرعت stream-up نزدیک شده است که عمدتاً به نفع استفاده از QUIC H3 از طریق CDN است.**

## H1 / H2 / H3

حال که حالت packet-up را داریم که تقریباً از تمام میدباکس‌های HTTP عبور می‌کند، بیایید در مورد یک موضوع جالب صحبت کنیم: **عبور QUIC H3 از CDN، یعنی همان عصر جدیدی که با SplitHTTP آغاز شد.** درک این بخش برای استفاده انعطاف‌پذیر از XHTTP بسیار مهم است.

یکی از ویژگی‌های بسیاری از میدباکس‌های HTTP، تبدیل نسخه HTTP است. به‌عنوان مثال، CDNها یا Nginx ممکن است ترافیک H3 دریافتی را به H1 یا H2 تبدیل کرده و سپس به مبدأ ارسال کنند. **یعنی سرور XHTTP ما می‌تواند فقط به درخواست‌های H1 و H2 گوش دهد و نیازی به گوش دادن به H3 ندارد، اما کلاینت XHTTP می‌تواند از H3 استفاده کند.**

**این رفتار پیش‌فرض سرور XHTTP است: فقط به پورت TCP گوش داده و ترافیک H1 و H2 را پردازش می‌کند.** هنگامی که TLS فعال باشد و در `alpn` فقط یک عنصر "h3" تنظیم شده باشد، سرور فقط با استفاده از quic-go به پورت UDP گوش داده و ترافیک H3 را پردازش می‌کند. اما در حال حاضر توصیه نمی‌شود این کار را انجام دهید، **بلکه بهتر است XHTTP را پشت یک Nginx یا Caddy واقعی پنهان کنید تا اثرانگشت آن کاهش یابد. این یکی از مزایای مهم XHTTP در مقایسه با سایر پروکسی‌های مبتنی بر QUIC است. ~~مزیت دیگر البته عبور از CDN است.~~** علاوه بر این، کنترل ازدحام (congestion control) در H3 در لایه کاربردی پیاده‌سازی شده است. از نظر تئوری می‌توانید الگوریتم کنترل ازدحام QUIC این نرم‌افزارهای پروکسی معکوس را تغییر داده و مجدداً کامپایل کنید. ~~تا کاری که برخی می‌خواهند (ارسال بی‌رویه بسته) را انجام دهید.~~

**برای کلاینت XHTTP:**
۱.  **هنگام فعال بودن TLS/REALITY، به‌صورت پیش‌فرض از H2 استفاده می‌شود.** در غیر این صورت از HTTP/1.1 استفاده می‌شود.
۲.  هنگام فعال بودن TLS، اگر `alpn` فقط شامل "http/1.1" باشد، از HTTP/1.1 استفاده می‌شود (اما Xray اجازه نمی‌دهد که این تنظیم، اثرانگشت تقلیدشده مرورگر uTLS را تغییر دهد).
۳.  **هنگام فعال بودن TLS، اگر `alpn` فقط شامل "h3" باشد، از quic-go H3 استفاده می‌شود.**
۴.  اما هنگام استفاده از Browser Dialer، نسخه HTTP خاص توسط مرورگر تعیین می‌شود (کل بخش `tlsSettings` بی‌اثر می‌شود).

**اگر می‌خواهید نسخه HTTP واقعی استفاده‌شده توسط کلاینت Xray، host، حالت XHTTP (mode)، اطلاعات جداسازی بالا‌رونده/پایین‌رونده و غیره را تأیید کنید، کافی است سطح لاگ را روی "info" تنظیم کنید.**

**پروکسی QUIC H3 از طریق CDN، حداقل در مورد XHTTP، اولین پیاده‌سازی گسترده بود و راه جدیدی گشود.** زیرا در برخی مناطق یا برای برخی ارائه‌دهندگان اینترنت، ممیزی روی H3 سخت‌گیرانه نیست. ~~البته برخی نیز آن را به شدت محدود می‌کنند.~~ حتی پس از توسعه حالت stream-up و کشف این که با افزودن تقلید هدر gRPC می‌توان از Cloudflare عبور کرد، این فقط روی H2 عمل می‌کرد. ~~به نظر می‌رسد ارزش این عصر جدید همچنان در حال افزایش است.~~

## XMUX

**با اشاره به H2 و H3، نمی‌توان از مالتی پلکسینگ آنها چشم‌پوشی کرد: هر دو ۰-RTT هستند.** تفاوت در این است که H3 مشکل مسدود شدن سر صف (head-of-line blocking) مربوط به TCP در H2 را ندارد و از مهاجرت اتصال (connection migration) پشتیبانی می‌کند، بنابراین تغییر شبکه توسط کلاینت باعث قطع اتصال نمی‌شود. ~~پس دوستانی که مرتباً RFCها را مطالعه می‌کنند خواهند پرسید:~~ چگونه می‌توان مالتی پلکسینگ آنها را به‌طور دقیق کنترل کرد؟ **ما یک رابط ساده اما قدرتمند طراحی کرده‌ایم به نام XMUX:**

*   `maxConcurrency`: حداکثر تعداد درخواست‌های پروکسی که می‌توانند به‌طور همزمان در یک اتصال TCP/QUIC وجود داشته باشند. **پس از رسیدن تعداد درخواست‌های پروکسی درون یک اتصال به این مقدار، core یک اتصال جدید ایجاد می‌کند** تا درخواست‌های پروکسی بیشتری را در خود جای دهد. **وقتی همه مقادیر XMUX صفر باشند، مقدار پیش‌فرض این پارامتر "16-32" است که هر بار به‌صورت تصادفی انتخاب می‌شود.**

*   `maxConnections`: حداکثر تعداد اتصالاتی که می‌توانند به‌طور همزمان وجود داشته باشند. **تا قبل از رسیدن به این تعداد اتصال، هر درخواست پروکسی جدید یک اتصال جدید ایجاد می‌کند.** پس از آن، core شروع به استفاده مجدد از اتصالات موجود می‌کند. این پارامتر با `maxConcurrency` در تضاد است و فقط یکی از آنها قابل انتخاب است. مقدار پیش‌فرض ۰ است (یعنی بدون محدودیت). امکان تعریف بازه نیز وجود دارد که هر بار به‌صورت تصادفی انتخاب می‌شود.

*   `cMaxReuseTimes`: یک اتصال حداکثر چند بار می‌تواند مورد استفاده مجدد قرار گیرد. **پس از استفاده مجدد به این تعداد، دیگر درخواست پروکسی جدیدی به آن اختصاص داده نمی‌شود** و پس از بسته شدن آخرین درخواست پروکسی داخلی، قطع می‌شود. مقدار پیش‌فرض ۰ است (یعنی بدون محدودیت). امکان تعریف بازه نیز وجود دارد که هر بار به‌صورت تصادفی انتخاب می‌شود.

*   `hMaxRequestTimes`: @xqzr متوجه شد که Nginx به‌صورت پیش‌فرض اجازه می‌دهد هر اتصال TCP/QUIC حداکثر ۱۰۰۰ درخواست HTTP را تجمیع کند. **وقتی همه مقادیر XMUX صفر باشند، مقدار پیش‌فرض این پارامتر "600-900" است که به‌صورت تصادفی انتخاب می‌شود.** در غیر این صورت مقدار پیش‌فرض آن ۰ است (یعنی بدون محدودیت). **این پارامتر بر اساس شمارش درخواست‌های HTTP عمل می‌کند.** به‌طور کلی، حالت stream-one فقط یک درخواست HTTP تولید می‌کند، stream-up دو درخواست و packet-up تعداد N درخواست تولید می‌کند. این شمارش دقیق نیست و همچنین GET در Golang دارای تلاش مجدد خودکار است، بنابراین توصیه نمی‌شود مقدار آن را در حداکثر ممکن تنظیم کنید. باید مقادیر CDNها و غیره را آزمایش کرد. **در حالت packet-up، اگر در حین ارسال POSTهای چرخشی بالا‌رونده از این مقدار تجاوز کند، به‌طور خودکار به یک اتصال TCP/QUIC دیگر سوئیچ می‌کند. این کار یک reuseTimes از آن اتصال جدید را مصرف می‌کند اما در concurrency شمرده نمی‌شود.** در واقع با سه مقدار پیش‌فرض XMUX، حالت‌های stream-* از حد مجاز تجاوز نمی‌کنند، فقط packet-up ممکن است تجاوز کند. و packet-up حالت پیش‌فرض برای H3 است. **بنابراین افزودن این پارامتر نیز عمدتاً به نفع H3 است.**

*   `hMaxReusableSecs`: @xqzr متوجه شد که Nginx به‌صورت پیش‌فرض اجازه می‌دهد هر اتصال TCP/QUIC حداکثر به مدت یک ساعت مورد استفاده مجدد قرار گیرد. **وقتی همه مقادیر XMUX صفر باشند، مقدار پیش‌فرض این پارامتر "1800-3000" است که به‌صورت تصادفی انتخاب می‌شود.** در غیر این صورت مقدار پیش‌فرض آن ۰ است (یعنی بدون محدودیت). **اتصالات TCP/QUIC پس از گذشت این مدت زمان، دیگر درخواست HTTP جدیدی دریافت نمی‌کنند** و پس از بسته شدن آخرین درخواست HTTP داخلی، قطع می‌شوند. در حالت packet-up، اگر در حین ارسال POSTهای چرخشی بالا‌رونده از این مقدار زمانی تجاوز کند، به‌طور خودکار به یک اتصال TCP/QUIC دیگر سوئیچ می‌کند. این کار یک reuseTimes از آن اتصال جدید را مصرف می‌کند اما در concurrency شمرده نمی‌شود.

*   `hKeepAlivePeriod`: در اتصالات H2/H3، کلاینت در زمان بیکاری هر چند ثانیه یک بار بسته نگهدارنده (keep-alive) ارسال می‌کند. مقدار پیش‌فرض ۰ است که معادل ۴۵ ثانیه برای H2 در کروم یا ۱۰ ثانیه برای H3 در quic-go است. این پارامتر تنها مورد در XMUX است که **اجازه تعریف بازه را نمی‌دهد** (تصادفی کردن این مقدار خود یک ویژگی شناسایی است) و **اجازه تعریف مقدار منفی را می‌دهد** (مثلاً با تنظیم -1 می‌توان بسته‌های نگهدارنده در زمان بیکاری را غیرفعال کرد). توصیه می‌شود آن را روی ۰ رها کنید.

پارامترهای ارائه‌شده توسط XMUX می‌توانند ترکیب‌های مختلفی از کاربردها را ایجاد کنند. به‌عنوان مثال، قبل از تست سرعت چندنخی، نیاز به تنظیم `"maxConcurrency": 1` است. برای «استفاده مجدد نامحدود» می‌توان `"maxConnections": 1` را تنظیم کرد. حتی اگر حوصله مطالعه ندارید نیز مشکلی نیست. **وقتی همه این مقادیر صفر باشند، سه مقدار پیش‌فرض ذکرشده در بالا اعمال می‌شوند که در عمل به معنای تعویض اتصال اصلی H2/H3 در بازه‌های زمانی است.** این کار بسیار روان است و تجربه «قطع جریان» ناشی از استفاده مداوم از یک اتصال مشترک در لایه‌های انتقال gRPC یا HTTP سنتی را ایجاد نمی‌کند. این پارامترها نیز می‌توانند از طریق `extra` به کلاینت ارسال شوند.

توجه: اگر هیچ چیزی پر نشود نیز معادل صفر بودن همه موارد است و سه مقدار پیش‌فرض اعمال می‌شوند. **اما اگر حتی یک مورد پر شود، سایر موارد دیگر مقدار پیش‌فرض نخواهند داشت و باید همه آنها را خودتان پر کنید.** به جز دو مورد اول، سایر موارد را می‌توان همزمان پر کرد.

**علاوه بر این، هنگام استفاده از XHTTP، mux.cool را فعال نکنید. همچنین سرور Xray در نسخه‌های جدید دارای بررسی است و فقط XUDP خالص را می‌پذیرد.**

برخی نقل‌قول‌ها درباره مقادیر پیش‌فرض XMUX:

> *   من به‌طور خاص دو گزینه را انتخاب کردم که خارج از TLS به‌راحتی قابل تشخیص نیستند: maxConcurrency به همراه cMaxReuseTimes (به جای maxConnections به همراه cMaxLifetimeMs). علاوه بر این، مقادیر پیش‌فرض هر دو این گزینه‌ها به صورت بازه و تصادفی انتخاب می‌شوند تا بیشترین حد ممکن از ویژگی‌های شناسایی بالقوه کاسته شود.
> *   من maxConcurrency را به جای maxConnections انتخاب کردم تا از تبدیل شدن تعداد اتصالات به یک الگوی ثابت جلوگیری کنم. البته اگر شما طرفدار مورد دوم هستید، می‌توانید آن را به‌صورت دستی تنظیم کنید.

> *   XMUX و Nginx اشیاء شمارش متفاوتی دارند. maxConcurrency و cMaxReuseTimes هر دو بر اساس «اتصالات پروکسی‌شده» شمارش می‌کنند. فقط stream-one یک درخواست HTTP تولید می‌کند، در حالی که stream-up یک بالا‌رونده و یک پایین‌رونده، یعنی دو درخواست تولید می‌کند و packet-up تعداد N درخواست تولید می‌کند.
> *   با این حال، مطمئن نیستم که stream-one در برخی شرایط به‌طور خودکار یک درخواست HTTP اضافی تولید کند یا خیر. **همچنین فکر می‌کنم دنبال کردن استفاده مجدد کامل از یک اتصال تا انتها چندان معنادار نیست، زیرا اگر شما ارائه‌دهنده خدمات اینترنتی بودید، شما نیز اتصالات قدیمی را محدود یا پاکسازی می‌کردید تا منابع توسط اتصالات قدیمی به‌تدریج اشغال نشوند. بنابراین پارامترهای پیش‌فرض XMUX به گونه‌ای هستند که استفاده مجدد را محدود کرده و اتصال را به‌طور دوره‌ای عوض می‌کنند.**

در ادامه مقاله نیز درباره برخی پارامترهای Nginx بحث شده است.

## STREAM-UP/ONE

بالاخره نوبت به حالت مهم دیگر XHTTP می‌رسد: **حالت stream-up یا «جریان بالا‌رونده، جریان پایین‌رونده». همانطور که از نامش پیداست، بالا‌رونده در این حالت نیز به صورت جریان‌ای است و بنابراین کارایی بالا‌رونده قربانی نمی‌شود.** این حالت در ابتدا برای REALITY توسعه داده شد، تا زمانی که متوجه شدیم با افزودن تقلید هدر gRPC، حتی H2 جریان‌ای بالا‌رونده نیز می‌تواند از Cloudflare عبور کند (**نیاز به فعال‌سازی پشتیبانی gRPC در پنل دارد**) و پشتیبانی نرم‌افزارهای پروکسی معکوس مانند Nginx نیز خوب است (**توصیه می‌شود برای Nginx از grpc_pass استفاده کنید، ساده و بی‌دردسر است**). بنابراین رفتار مقدار پیش‌فرض "auto" برای `mode` به این صورت است:

*   **کلاینت:** **در صورت TLS H2 از stream-up، در صورت REALITY از stream-one** (اگر `downloadSettings` تنظیم شده باشد از stream-up استفاده می‌کند)، **در غیر این صورت از packet-up استفاده می‌کند.**
*   **سرور:** به‌صورت پیش‌فرض هر سه حالت را می‌پذیرد. **اگر روی یک حالت خاص تنظیم شود، فقط آن حالت را می‌پذیرد.** حالت "stream-up" استثنا است و علاوه بر خود، stream-one را نیز می‌پذیرد.

> سازگاری stream-up کمی بهتر از stream-one است. برخی از دوستان در گروه گزارش داده‌اند که با فعال‌سازی جریان بالا‌رونده در Cloudflare Tunnel می‌توان از stream-up استفاده کرد، اما برای استفاده از stream-one باید یک گزینه دیگر نیز فعال شود (شاید به دلیل تقلید SSE؟). همچنین دیده شده که یک CDN (اسمش یادم نیست) با stream-one به شدت محدودیت سرعت اعمال می‌کند اما با stream-up اینطور نیست.

**روش پیاده‌سازی آنها به این صورت است (غیرتوسعه‌دهندگان می‌توانند این بخش را نادیده بگیرند):**

*   برای stream-up، کافی است ارسال بسته‌ای بالا‌رونده packet-up به جریان‌ای `POST /yourpath/sameUUID` تغییر کند. `Referer: ...?x_padding=XXX...` نیز وجود دارد.
*   برای stream-one، کافی است `POST /yourpath/` ارسال شود و پاسخ همان پایین‌رونده است (جریان دوطرفه). هر دو هدر درخواست و پاسخ دارای header padding هستند.
*   توجه: در stream-one اگر `/yourpath` وارد شود، در واقع درخواست برای `/yourpath/` ارسال می‌شود. اگر در انتها `/` نباشد، به‌طور خودکار اضافه می‌شود.
*   **در بالا‌رونده هر دو حالت، به‌صورت پیش‌فرض `Content-Type: application/grpc` وجود دارد تا تقلید gRPC انجام شود (می‌توان با تنظیم `noGRPCHeader` آن را غیرفعال کرد).**
*   هدرهای پاسخ سرور برای پایین‌رونده کاملاً مشابه مورد ۱ در packet-up است. در stream-one صحنه عجیبی رخ می‌دهد که SSE به درخواست gRPC پاسخ می‌دهد! در صورت بروز مشکل، می‌توان `noSSEHeader` را امتحان کرد.

> آزمایش‌های مرتبط در https://github.com/XTLS/Xray-core/discussions/4113#discussioncomment-11682823 نشان داد که Cloudflare اتصالات HTTP که در پایین‌رونده به مدت ۱۰۰ ثانیه فاقد داده واقعی باشند را قطع می‌کند که این امر باعث قطع شدن جهت بالا‌رونده در stream-up می‌شود. **بنابراین این [PR](https://github.com/XTLS/Xray-core/pull/4306) پارامتر `scStreamUpServerSecs` را به سرور اضافه کرد. مقدار پیش‌فرض آن "20-80" است که به‌صورت تصادفی انتخاب می‌شود. سرور هر چند وقت یک بار (در این بازه) `xPaddingBytes` بایت داده ارسال می‌کند تا اتصال را زنده نگه دارد.**
>
> می‌توان `"scStreamUpServerSecs": -1` را تنظیم کرد تا این مکانیسم غیرفعال شود. در این حالت سرور حتی هدرهای پاسخ را نیز به‌موقع ارسال نمی‌کند و رفتار مشابه نسخه‌های قبلی خواهد بود.

**پس دوستانی که مرتباً از gRPC استفاده می‌کنند خواهند پرسید: stream-up نسبت به لایه انتقال gRPC چه مزایایی دارد؟**

*   اولی به هیچ کتابخانه gRPC خاصی نیاز ندارد و **کارایی بهتری دارد.**
*   ترافیک پایین‌رونده در اولی یک درخواست GET مستقل است و **تحت تاثیر محدودیت‌های ترافیکی CDN روی gRPC قرار نمی‌گیرد.**
*   اولی دارای ویژگی‌های افزوده‌ای مانند header padding، **XMUX**، جداسازی بالا‌رونده/پایین‌رونده و غیره است و همچنین مکانیسم extra را معرفی کرده که امکان اشتراک‌گذاری تمام پارامترها را فراهم می‌کند و **بالغ‌تر است.**

البته، مزایای XHTTP نسبت به WebSocket یا HTTPUpgrade، به‌جز **«عدم داشتن ویژگی شناسایی واضح ALPN با مقدار http/1.1»**، احتمالاً تا اینجای مقاله برای شما نیز روشن شده است و نیازی به فهرست کردن همه آنها نیست. ~~اصلاً هم زیاد هستند و فهرست کردنشان سخت است.~~

## جداسازی بالا‌رونده و پایین‌رونده

**و بالاخره نوبت به عصر جدید دیگری می‌رسد: جداسازی بالا‌رونده و پایین‌رونده.** تقریباً می‌دانیم که تشخیص دیوار آتش بزرگ برای ویژگی‌های ترافیکی مانند TLS in TLS بر اساس یک اتصال واحد انجام می‌شود. **پس اگر ما بالا‌رونده و پایین‌رونده را به سیستم‌های بازرسی متفاوتی منتقل کنیم، مثلاً بالا‌رونده روی IPv4 TCP و پایین‌رونده روی IPv6 UDP اجرا شود، دیوار آتش بزرگ نمی‌تواند به سرعت واکنش نشان دهد.** از آنجایی که سرور XHTTP تنها بر اساس UUID تصادفی تولیدشده در مسیر (path)، بالا‌رونده و پایین‌رونده را به هم مرتبط می‌کند، **حالت‌های packet-up و stream-up به‌طور ذاتی قابلیت جداسازی واقعی بالا‌رونده و پایین‌رونده را دارند.** همچنین از آنجایی که XHTTP می‌تواند از انواع CDNها عبور کند و با REALITY و غیره ترکیب شود، گزینه‌های بی‌شماری برای پیاده‌سازی وجود دارد. برای کلاینت، باید `downloadSettings` تنظیم شود:

```jsonc
"xhttpSettings": {
    "host": "example.com",
    "path": "/yourpath", // باید یکسان باشد
    "mode": "auto",
    "extra": {
        "headers": {
            // "کلید": "مقدار"
        },
        "xPaddingBytes": "100-1000",
        "noGRPCHeader": false, // فقط برای stream-up/one و فقط کلاینت
        "noSSEHeader": false, // فقط سرور
        "scMaxEachPostBytes": 1000000, // فقط packet-up
        "scMinPostsIntervalMs": 30, // فقط packet-up و فقط کلاینت
        "scMaxBufferedPosts": 30, // فقط packet-up و فقط سرور
        "scStreamUpServerSecs": "20-80", // فقط stream-up و فقط سرور
        "xmux": { // عمدتاً برای h2/h3 و فقط کلاینت
            "maxConcurrency": "16-32",
            "maxConnections": 0,
            "cMaxReuseTimes": 0,
            "hMaxRequestTimes": "600-900",
            "hMaxReusableSecs": "1800-3000",
            "hKeepAlivePeriod": 0
        },
        "downloadSettings": { // فقط کلاینت
            "address": "", // یک دامنه/IP دیگر
            "port": 443,
            "network": "xhttp", // نباید حذف شود
            "security": "tls",
            "tlsSettings": {
                ...
            },
            "xhttpSettings": {
                "path": "/yourpath", // باید یکسان باشد
                ...
            },
            "sockopt": {} // اگر `penetrate` در `sockopt` بالا‌رونده true باشد، توسط آن جایگزین می‌شود
        }
    }
}
```

**همانطور که مشاهده می‌کنید، `downloadSettings` در واقع یک `streamSettings` کاملاً جدید است** اما دارای `address` و `port` مشابه خروجی VLESS است تا به ورودی دیگری اشاره کند. واضح است که `network` در آن باید "xhttp" باشد (نباید حذف شود) و `security` می‌تواند "tls" یا "reality" باشد. **مورد `sockopt` نیز قابل اشتراک‌گذاری است، اما دریافت‌کننده می‌تواند در `sockopt` بالا‌رونده، `penetrate` را true تنظیم کند تا آن را جایگزین کند (مناسب برای مواردی مانند تنظیم `mark`). به جز این استثنا، پیکربندی پایین‌رونده در جداسازی کاملاً مستقل است و هیچ یک از تنظیمات بالا‌رونده را به ارث نمی‌برد.** همچنین، حتی اگر در هر دو جهت بالا‌رونده و پایین‌رونده XMUX پر نشده باشد و مقادیر پیش‌فرض اعمال شوند، مقادیر تصادفی انتخاب‌شده در بازه برای هر جهت مستقل و نامرتبط با جهت دیگر خواهد بود. **بدین ترتیب با گذشت زمان، الگوی استفاده مجدد در دو جهت کاملاً نامتقارن شده و زمان تعویض اتصال اصلی نیز متفاوت خواهد بود که اثر ضدتحلیل بهتری دارد.** زیرا اگر دیوار آتش بزرگ بخواهد به جداسازی بالا‌رونده/پایین‌رونده حمله کند، قطعاً «همزمانی زمان آغاز اتصال اصلی» مهم‌ترین نقطه شروع خواهد بود. بنابراین در آینده، XHTTP اجازه خواهد داد که از ابتدا اتصالات بالا‌رونده و پایین‌رونده در زمان‌های متفاوتی آغاز شوند.

در واقع، اگر از CDN استفاده می‌کنید، حتی بدون تغییر پیکربندی سرور نیز می‌توانید جداسازی بالا‌رونده/پایین‌رونده را اجرا کنید. **مثلاً می‌توانید یک IPv4 بهینه‌شده برای TLS H2 و یک IPv6 بهینه‌شده برای QUIC H3 انتخاب کنید.** علاوه بر این، اکثر CDNها از «دامنه‌فرستی هم‌دامنه» پشتیبانی می‌کنند. مثلاً در بالا‌رونده `serverName` را "a.example.com"، در پایین‌رونده `serverName` را "b.example.com" و در هر دو جهت `host` را "c.example.com" قرار دهید تا **حتی از نظر خارجی نیز SNI متفاوت به نظر برسد.** البته اگر از قبل دو دامنه مجزا دارید، حتی بهتر است. اگر هیچ دامنه‌ای ندارید، می‌توانید دو VPS راه‌اندازی کنید و دو REALITY جداگانه برای بازی با جداسازی بالا‌رونده/پایین‌رونده ایجاد کنید: **چه با CDN به همراه پروکسی معکوس و چه با REALITY به همراه fallback، تا زمانی که هر دو جهت در نهایت با یک مسیر (path) مشخص به یک ورودی XHTTP روی یک VPS واحد برسند، کار خواهد کرد.** در کل، از آنجایی که XHTTP تقریباً همه‌جا قابل استفاده است، ترکیب‌های بی‌شماری را می‌توان ایجاد کرد. تنها محدودیت، اندازه خلاقیت شماست.

**مثلاً پس از معرفی جداسازی بالا‌رونده/پایین‌رونده، بسیاری از افراد در واقع از آن برای اختصاص دادن بالا‌رونده به خطوط با رفت بهینه و پایین‌رونده به خطوط با برگشت بهینه استفاده کردند که به خودی خود کاربردی و مفید است.**

مثلاً می‌توانید برای بالا‌رونده `"maxConnections": 1` و برای پایین‌رونده `"maxConcurrency": 1` تنظیم کنید تا **داده‌های کم حجم بالا‌رونده همه از یک اتصال زیرین مشترک عبور کنند، در حالی که داده‌های پرحجم پایین‌رونده روی اتصالات زیرین مختلف توزیع شوند.** این کار همزمان دارای مزایای ضدسانسور، تاخیر کم و سرعت بالا است و کمی شبیه به سوئیچ عمل می‌کند. ترکیب آن با Vision Seed نتیجه بهتری دارد.

در بالا نمونه‌ای از پیکربندی شامل تمام پارامترها ارائه شد. **هدف اصلی این بود که محل صحیح نوشتن هر پارامتر نشان داده شود.** همچنین به توضیح برخی پارامترهایی که در متن قبلاً به طور مفصل توضیح داده نشده بودند می‌پردازیم:

*   **`extra` طرح اشتراک‌گذاری JSON خام برای تمام پارامترها به جز `host`، `path` و `mode` است. وقتی `extra` وجود دارد، فقط این چهار مورد اعمال می‌شوند. همچنین در لینک‌های اشتراک‌گذاری فقط این چهار مورد وجود دارند و معمولاً در GUIها نیز فقط این چهار مورد نمایش داده می‌شوند. زیرا پارامترهای درون `extra` نسبتاً کم‌کاربردتر هستند و باید مستقیماً توسط ارائه‌دهنده سرویس به کلاینت ارسال شوند، نه اینکه کلاینت به طور دلخواه آنها را تغییر دهد.**
    توضیح تکمیلی: منظور از «ارسال»، ارسال توسط انسان است. یعنی ارائه‌دهنده سرویس `extra` را می‌نویسد و کل آن را در لینک اشتراک‌گذاری قرار می‌دهد و کلاینت می‌تواند مستقیماً آن را به عنوان `extra` خود استفاده کند.

*   رفتار `host` مشابه سایر لایه‌های انتقال مبتنی بر HTTP در Xray است. **اولویت ارسال host توسط کلاینت به این صورت است: `host` > `serverName` > `address`**. اگر سرور `host` را تنظیم کرده باشد، مقدار ارسالی از سمت کلاینت را بررسی می‌کند، در غیر این صورت بررسی نمی‌کند. توصیه می‌شود اگر ضرورتی ندارد، آن را تنظیم نکنید. `host` را نباید درون `headers` قرار داد.

> **اگر می‌خواهید نسخه HTTP واقعی استفاده‌شده توسط کلاینت Xray، host، حالت XHTTP (mode)، اطلاعات جداسازی بالا‌رونده/پایین‌رونده و غیره را تأیید کنید، کافی است سطح لاگ را روی "info" تنظیم کنید.**

## فراتر از REALITY

**اوایل سال گذشته من بازگشتم و REALITY را نوشتم که ~~همه چیز را تحت الشعاع قرار داد~~ و چندین نقطه دردناک را یکجا حل کرد.** ~~بعدش هم تقریباً هر روز به کلاب می‌رفتم و زیاد بهش رسیدگی نکردم~~، حتی مقاله‌اش هم تا الان نوشته نشده باقی ماند. ~~مقاله XUDP UoT Migration هم بیشتر可惜 است، پارسال تقریباً نوشته شده بود ولی پخش نشد.~~ **ناگهان متوجه شدیم که REALITY به یک گزینه اصلی برای اتصال مستقیم تبدیل شده است.** اغلب می‌بینیم که وقتی یک IP مسدود می‌شود، در نظرات توصیه می‌کنند که به REALITY مهاجرت کنید. اعتبار آن آشکار است. ~~حتی به دلیل پایداری بیش از حد، اینجا دیگر مثل سابق شلوغ نیست.~~ اگرچه Xray برای سایر لایه‌های انتقال نیز بهینه‌سازی‌ها و بهبودهای زیادی انجام داده است، **اما XHTTP اولین لایه انتقال بومی Xray است. ~~اولین کارش هم یک حرکت بزرگ بود.~~** اکنون که این مقاله را خوانده‌اید، می‌دانید که XHTTP می‌تواند همراه با REALITY استفاده شود:

**منظور از «فراتر از REALITY» جایگزینی REALITY نیست، بلکه محبوبیت بیشتر از REALITY است.** بدون اغراق می‌توان گفت، همانطور که همیشه سبک Xray بوده، ظهور XHTTP باعث شده سایر لایه‌های انتقال مبتنی بر HTTP همگی تحت الشعاع قرار گیرند. این همان عصر همه‌کاره‌بودن XHTTP است. XHTTP از آخرین پروتکل موفق، یعنی REALITY، محبوب‌تر خواهد شد زیرا ویژگی‌هایش از قبل دامنه پوشش گسترده‌تری را برای آن تعیین کرده است.

در پایان، امیدوارم ظهور XHTTP بتواند کمی شوک مثبت به جامعه وارد کند، همانطور که Xray در طول تاریخ بارها این کار را انجام داده است: **انقلاب و نوآوری، همیشه اعتقاد Xray بوده است.**

### اگر این مقاله برای شما مفید بود، از یک [REALITY NFT](https://opensea.io/assets/ethereum/0x5ee362866001613093361eb8569d59c4141b76d1/2) حمایت کنید:

*   تعداد کل: ۱۰۰۰ عدد. قیمت ۱۰۰ عدد اول: ۰.۰۱ ETH. پس از آن قیمت افزایش می‌یابد. **دریافت نسخه اولیه مانند پیدا کردن گنج است.**
*   ~~یک مقاله معرفی REALITY در روزهای آینده منتشر خواهد شد.~~ تبدیل به مقاله معرفی XHTTP شد.
*   **دارندگان REALITY NFT واجد شرایط دسترسی زودهنگام به کلاینت جدید Xray و همچنین ایردراپ NFT بعدی خواهند بود.**
*   دارندگان Project X NFT در تاریخ ۲۰۲۵.۱.۱ دو عدد REALITY NFT هدیه دریافت خواهند کرد.

با حمایت شما، Project X NFT پنج تا شش برابر افزایش قیمت داشته است. برای دوستانی که در زمان عرضه اولیه Project X NFT مردد بودند، این فرصت خوبی برای جبران است.

### البته اگر توانایی مالی دارید، حمایت از [Project X NFT](https://github.com/XTLS/Xray-core/discussions/3633) حتی سپاس‌گذارتر است:

*   آدرس برای ETH/USDT/USDC: `0xDc3Fe44F0f25D13CACb1C4896CD0D321df3146Ee`
*   Project X NFT: [Announcement of NFTs by Project X #3633](https://github.com/XTLS/Xray-core/discussions/3633)
*   REALITY NFT: https://opensea.io/assets/ethereum/0x5ee362866001613093361eb8569d59c4141b76d1/2
*   گاهی هزینه gas در شبکه ETH به شدت بالا می‌رود. می‌توانید ابتدا آن را به علاقه‌مندی‌های خود اضافه کنید و زمانی که gas fee نسبتاً normal است خریداری کنید.

اگر هنوز نقطه مبهمی وجود دارد، لطفاً در بخش نظرات زیر مطرح کنید تا مقاله را به‌روز کنم. ~~توصیه می‌شود نسخه ترجمه شده را خیلی زود منتشر نکنید.~~
