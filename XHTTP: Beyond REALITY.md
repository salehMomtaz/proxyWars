## &rlm; # چینی | [روسی](https://github.com/XTLS/Xray-core/discussions/4113#discussioncomment-11468947)&lrm;

# &rlm;XHTTP: فراتر از REALITY&lrm;

&rlm;**خلاصه توسعه:** در اواسط سال &rlm;۲۰۲۴، @mmmray و @ll11l1lIllIl1lll و دیگران بر اساس اصل «packet-up / stream-down» و جزئیات پیاده‌سازی شرح‌داده‌شده توسط @RPRX، SplitHTTP را توسعه دادند. **این اولین بار بود که بدون قربانی کردن کارایی downlink، از اکثر میدباکس‌های HTTP عبور می‌کرد و همچنین اولین بار بود که QUIC H3 را در مقیاس بزرگ از طریق CDN پیاده‌سازی می‌کرد و عصری جدید را آغاز می‌کرد.** پشتیبانی از Browser Dialer، header padding برای کاهش ویژگی‌های شناسایی، XMUX برای کنترل multiplexing و فعال‌سازی REALITY نیز به ترتیب اضافه شدند. سپس @RPRX توسعه را بر عهده گرفت و **جداسازی واقعی uplink/downlink را پیاده‌سازی کرد و نام آن را به XHTTP تغییر داد. به عنوان مثال، uplink و downlink می‌توانند به ترتیب IPv6 CDN H3 و IPv4 REALITY H2 باشند (حتی IPهای مبدا می‌توانند متفاوت باشند). ~~این بار نیز عصری جدید آغاز شد.~~** بلافاصله پس از آن، حالت stream-up (جریان بالا‌رونده) که کارایی uplink را قربانی نمی‌کند، طرح extra برای اشتراک‌گذاری تمام جزئیات پیکربندی، و همچنین camouflage هدر gRPC به طور پیش‌فرض برای حالت stream-up توسعه داده شد. **این امر عبور H2 stream-up از CDN را ممکن ساخت و جایگزین لایه انتقال سنتی gRPC شد. ~~اگر از ابتدا می‌دانستیم این کار ممکن است، هرگز gRPC را نمی‌ساختیم.~~** در نهایت، لایه انتقال HTTP به عنوان حالت stream-one در XHTTP ادغام شد و آن نیز از ویژگی‌هایی مانند header padding، XMUX و camouflage هدر gRPC بهره‌مند شد. **بدین ترتیب، ما به XHTTP کامل دست یافتیم: عبور از میدباکس‌ها به روش‌های مختلف، جداسازی uplink/downlink، XMUX روان و هر آنچه نیاز دارید. عصر همه‌کاره بودن XHTTP رسماً آغاز شده است.** &lrm;

&rlm;این متن در ابتدا به عنوان یک مستند ویرایش‌شده در نظر گرفته شده بود، اما ناخواسته به یک مقاله تبدیل شد. هدف این مقاله کمک به درک عمیق اصول و طراحی XHTTP برای استفاده بهتر از آن است.&lrm;

&rlm;**اگر قصد حمایت مالی از پروژه Xray یا جمع‌آوری NFT دارید، اطلاعات مربوطه در انتهای مقاله آمده است. از حمایت شما سپاسگزاریم!**&lrm;

## &rlm; راهنمای سریع شروع&lrm;

&rlm;با وجود پارامترهای نسبتاً زیاد XHTTP، همه آنها به طور پیش‌فرض تنظیم شده‌اند. اگر فقط می‌خواهید از XHTTP استفاده کنید، کافی است این شش مرحله را دنبال کنید:&lrm;

&rlm;۱. چه برای TLS و چه برای REALITY، **به طور کلی برای پیکربندی XHTTP فقط کافی است `path` را پر کنید و بقیه موارد را خالی بگذارید.**&lrm;
&rlm;۲. اگر سرور از QUIC H3 پشتیبانی می‌کند، **در سمت کلاینت کافی است `alpn` را روی "h3" تنظیم کنید تا از QUIC استفاده شود.**&lrm;
&rlm;۳. برای استفاده از IPهای بهینه‌شده CDN، در سمت کلاینت `address` را با IP و `serverName` (SNI) را با دامنه پر کنید.&lrm;
&rlm;۴. اگر به Cloudflare متصل نمی‌شوید، **پشتیبانی gRPC را در پنل Cloudflare فعال کنید.**&lrm;
&rlm;۵. اگر از Nginx عبور نمی‌کند، **در Nginx دستور `proxy_pass` را به `grpc_pass` تغییر دهید.**&lrm;
&rlm;۶. اگر از سایر CDNها یا نرم‌افزارهای پروکسی عبور نمی‌کند، پیشنهاد می‌شود `mode` را روی "packet-up" تنظیم کنید که بیشترین سازگاری را دارد.&lrm;

&rlm;**XHTTP به طور پیش‌فرض multiplexing دارد. latency آن نسبت به Vision کمتر است، اما در speedtest چندنخی ممکن است عملکرد پایین‌تری نشان دهد، مگر اینکه قبل از تست `"maxConcurrency": 1` تنظیم شده باشد. به بخش XMUX مراجعه کنید.**&lrm;

&rlm;Cloudflare اتصالات HTTP downlink را که بیش از ۱۰۰ ثانیه فاقد داده واقعی باشند، قطع می‌کند. برای حفظ اتصالات بلندمدت پروکسی، باید در لایه application اقدام به keepalive کنید؛ مثلاً برای SSH باید در تنظیمات sshd مقدار `ClientAliveInterval` را تنظیم کنید.&lrm;

&rlm;این مقاله می‌تواند به عنوان یک مستند پیشرفته استفاده شود. تقریباً تمام مواردی که می‌خواهید درباره XHTTP بدانید را پوشش می‌دهد و هر پارامتر را توضیح می‌دهد. **در انتهای مقاله نیز یک نمونه پیکربندی شامل تمام پارامترها به همراه توضیح کاربرد هر یک ارائه شده است. اگر درباره پارامتری مطمئن نیستید، کافی است نام آن را در متن جستجو کنید تا توضیح مفصل آن را بیابید.**&lrm;

## &rlm;دو منطق پایه‌ای&lrm;

&rlm;همانطور که [REALITY](https://github.com/XTLS/REALITY) می‌تواند خود را به عنوان وبسایت دیگری جا بزند، **منطق اساسی ضد سانسور، افزایش «خسارت جانبی» (collateral damage) برای سانسورچی است تا او از اعمال بلوک‌اژ سریع خودداری کند.** سال‌ها پیش نیز من به TLS و همچنین مخفی‌سازی ویژگی‌های زمانی و طولی ترافیک روی TLS خوش‌بین بودم. سال‌ها تجربه به ما نشان داده که دیوار آتش کل IP یک CDN بزرگ را به طور دائمی مسدود نمی‌کند، زیرا این کار بسیاری از وبسایت‌های معمولی را تحت تأثیر قرار می‌دهد. **بنابراین هدف اولیه ما برای XHTTP این بود که آن را پشت انواع مختلف CDNها پنهان کنیم.** با این حال، CDNها برای محافظت از سرورهای مبدا در برابر حملات، به جز موارد خاصی مانند WS و gRPC، معمولاً کل درخواست HTTP را کش کرده و سپس به مبدا ارسال می‌کنند. بسیاری از میدباکس‌های HTTP نیز به طور پیش‌فرض همین رفتار را دارند. بر این اساس، پروتکل Meek در Tor ترافیک رفت و برگشت را در قالب درخواست‌های HTTP مجزا بسته‌بندی می‌کند تا از این میدباکس‌ها عبور کند، اما سرعت آن بسیار پایین است زیرا از «جریان پایین‌رونده» (stream-down) XHTTP استفاده نمی‌کند. **«جریان پایین‌رونده» چیست؟ تصور کنید که در حال دانلود یک فایل بزرگ از یک وبسایت هستید. CDN کش را نمی‌زند و برای دریافت فایل به مبدا مراجعه می‌کند، اما واضح است که مانند uplink نمی‌تواند کل فایل را کش کند و شما را معطل نگه دارد، بلکه به محض رسیدن داده از مبدا، آن را بلافاصله به شما منتقل می‌کند. این اساس «جریان پایین‌رونده» XHTTP است و مهم‌تر از همه، تضمین می‌کند که سرعت downlink به حداکثر برسد.** در مورد uplink، برای حفظ سازگاری، XHTTP ابتدا «بسته‌بندی بالا‌رونده» (packet-up) را پیاده‌سازی کرد؛ یعنی ترافیک uplink را در قالب درخواست‌های POST مجزا بسته‌بندی می‌کند. کارایی این روش قطعاً کاهش می‌یابد، اما خوشبختانه برای نیازهای معمولی پروکسی، ترافیک uplink بسیار کم است. **پس از آن، من «جریان بالا‌رونده» (stream-up) را اضافه کردم و متوجه شدیم که با اضافه کردن camouflage هدر gRPC، حتی می‌توان H2 stream-up را از Cloudflare عبور داد. از سوی دیگر، پس از چندین دور بهینه‌سازی «بسته‌بندی بالا‌رونده»، سرعت آن حتی به stream-up نزدیک شده است.**&lrm;

&rlm;به طور گذرا به موضوع اخیراً داغ‌شده «سوءاستفاده از CDN» نیز می‌پردازیم: واضح است که CDNهایی که از stream-up پشتیبانی نمی‌کنند، قصد نداشته‌اند که از آنها برای راه‌اندازی سرویس پروکسی استفاده شود، اما ما برای مقابله با دیوار آتش، کاوش، توسعه و استفاده از راه‌های جدید تا حد امکان، اقدامی منطقی و ضروری است و چاره‌ای جز این نداریم. افزایش «خسارت جانبی» سانسورچی مستلزم آن است که ما خود را با سرویس‌های «عادی» ادغام کنیم و این امر اجتناب‌ناپذیر است. **ساده‌ترین مثال را بزنیم: اگر روزی لیست سفید IP اعلام شود و IPهای CDN در آن قرار گیرند، آیا از آن استفاده می‌کنید یا خیر؟ قواعد دنیای واقعی در حوزه ضد سانسور قابل اعمال نیستند، اما متأسفانه برخی هنوز این موضوع ساده را درک نکرده‌اند.** اگر هنوز متقاعد نشده‌اید، باید گفت که لایه انتقالی مانند WebSocket امروزه تنها برای استفاده با CDN وجود دارد. توسعه‌دهندگان می‌توانند آن را حذف کنند و کاربران می‌توانند به توسعه‌دهندگان پیشنهاد حذف آن را بدهند تا با اقدام عملی خود ثابت کنند که «سوءاستفاده از CDN» صرفاً بهانه‌ای دیگر برای حملات حسادت‌آمیز روزانه به Xray - که برای ارضای برخی تمایلات بیمارگونه انجام می‌شود - نیست. ~~در عین حال، عملشان خلاف گفتارشان است~~ و در عین حال به وضوح می‌توان ذات ریاکارانه برخی افراد را مشاهده کرد.&lrm;

## &rlm;  PACKET-UP&lrm;

&rlm;**این بخش دارای جزئیات فنی زیادی است. افراد غیرتوسعه‌دهنده می‌توانند فقط قسمت‌های پررنگ را بخوانند.**&lrm;

&rlm;**برای حالت «بسته‌بندی بالا‌رونده، جریان پایین‌رونده» (packet-up mode) که بیشترین سازگاری را در XHTTP دارد، طراحی به این صورت است:**&lrm;

&rlm;۱. **کلاینت `POST /yourpath/sameUUID/seq` را برای ارسال داده uplink ارسال می‌کند:**&lrm;
&rlm;   - UUID به صورت تصادفی تولید می‌شود و باید با UUIDای که برای downlink استفاده می‌شود یکسان باشد تا سرور بتواند آنها را مرتبط کند. اگر سرور نتواند در مدت ۳۰ ثانیه ارتباط را برقرار کند، session قطع خواهد شد.&lrm;
&rlm;   - seq از ۰ شروع می‌شود. باید body درخواست POST قبلی کاملاً ارسال شود (اما نیازی به انتظار برای پاسخ نیست) و سپس درخواست بعدی ارسال گردد.&lrm;
&rlm;   - ممکن است درخواست‌های POST با کمی بی‌ترتیبی به سرور برسند. سرور بر اساس seq آنها را مرتب می‌کند. به طور پیش‌فرض حداکثر ۳۰ درخواست POST کش می‌شود و در صورت تجاوز از حد، اتصال قطع می‌شود.&lrm;
&rlm;   - توجه داشته باشید که UUID و seq در مسیر (path) قرار دارند، نه در query string، تا از بروز مشکلات عجیب جلوگیری شود.&lrm;

&rlm;۲. **کلاینت `GET /yourpath/sameUUID` را برای راه‌اندازی downlink ارسال می‌کند. هدرهای پاسخ سرور شامل موارد زیر است:**&lrm;
&rlm;   - `X-Accel-Buffering: no` برای اطلاع به میدباکس‌ها جهت غیرفعال کردن بافرینگ.&lrm;
&rlm;   - `Cache-Control: no-store` برای اطلاع به میدباکس‌ها جهت عدم نیاز به کش.&lrm;
&rlm;   - `Content-Type: text/event-stream` برای camouflage به عنوان server-sent events (سازگاری بهتر؛ می‌توان با تنظیم `noSSEHeader` آن را غیرفعال کرد).&lrm;
&rlm;   - در صورت استفاده از HTTP/1.1 باید `Transfer-Encoding: chunked` نیز وجود داشته باشد. برای H2/H3 نیازی نیست.&lrm;

&rlm;۳. برای جلوگیری از محدودیت‌های CORS در هنگام استفاده از [Browser Dialer](https://xtls.github.io/config/features/browser_dialer.html)، سرور برای تمام پاسخ‌های GET و POST هدرهای زیر را شامل می‌شود:&lrm;
&rlm;   - `Access-Control-Allow-Origin: *`&lrm;
&rlm;   - `Access-Control-Allow-Methods: GET, POST`&lrm;

&rlm;۴. **برای حل مشکل ویژگی‌های طول ثابت هدرهای درخواست و پاسخ HTTP:**&lrm;
&rlm;   - هدر درخواست ارسالی از سمت کلاینت به طور پیش‌فرض شامل `Referer: ...?x_padding=XXX...` است (قرار دادن padding در Referer برای جلوگیری از ایجاد درخواست‌های OPTIONS غیرضروری توسط Browser Dialer است). طول پیش‌فرض آن بین ۱۰۰ تا ۱۰۰۰ کاراکتر است و در هر درخواست به صورت تصادفی انتخاب می‌شود. سرور به طور پیش‌فرض بررسی می‌کند که آیا این مقدار در محدوده مجاز سرور قرار دارد یا خیر.&lrm;
&rlm;   - هدر پاسخ ارسالی از سمت سرور به طور پیش‌فرض شامل `X-Padding: XXX...` است. طول پیش‌فرض آن بین ۱۰۰ تا ۱۰۰۰ کاراکتر است و در هر پاسخ به صورت تصادفی انتخاب می‌شود.&lrm;
&rlm;   - **این همان header padding است که من بارها به آن اشاره کرده‌ام و مربوط به تنظیم `xPaddingBytes` است.**&lrm;
&rlm;   - پیشنهاد قرار دادن padding درخواست در `Referer` توسط @rPDmYQ مطرح شد و انتخاب `XXX` نیز توسط او انجام گرفت. `X` در Huffman encoding معادل ۸ بیت است.&lrm;

&rlm;**بسته‌بندی بالا‌رونده و `Referer: ...?x_padding=XXX...` لاگ‌های نسبتاً زیاد و طولانی تولید می‌کنند. می‌توانید در نرم‌افزار پروکسی معکوس (reverse proxy) تنظیم کنید که آنها را ثبت نکند.**&lrm;

&rlm;علاوه بر این، مانند سایر لایه‌های انتقال Xray، سرور هدر `X-Forwarded-For` را نیز می‌پذیرد تا IP واقعی کلاینت را به دست آورد. همچنین بر اساس `host` تنظیم‌شده در سرور، host ارسالی از سمت کلاینت بررسی می‌شود (توصیه شخصی من این است که بدون دلیل خاصی آن را تنظیم نکنید، زیرا از قبل در مسیر پنهان شده است).&lrm;

&rlm;**این ساده‌ترین و ضروری‌ترین فرآیند حالت packet-up است. اما یک سوال کوچک باقی می‌ماند: چگونه درخواست‌های POST را به طور مشخص پیاده‌سازی و محدود کنیم؟ سه پارامتر اختصاصی وجود دارد:**&lrm;

&rlm;- `scMaxEachPostBytes`: حداکثر مقدار داده‌ای که کلاینت می‌تواند در هر درخواست POST حمل کند. مقدار پیش‌فرض ۱۰۰۰۰۰۰ است (یعنی ۱ مگابایت). این مقدار باید کمتر از حداکثر مقدار مجاز توسط CDN یا سایر میدباکس‌های HTTP باشد. سرور نیز درخواست‌های POST بزرگ‌تر از این مقدار را رد می‌کند.&lrm;
&rlm;- `scMinPostsIntervalMs`: فقط برای کلاینت. بر اساس هر درخواست پروکسی مجزا، حداقل فاصله زمانی بین ارسال درخواست‌های POST توسط کلاینت. مقدار پیش‌فرض ۳۰ میلی‌ثانیه است.&lrm;
&rlm;- `scMaxBufferedPosts`: فقط برای سرور. بر اساس هر درخواست پروکسی مجزا، حداکثر تعداد درخواست‌های POSTی که سرور می‌تواند کش کند. مقدار پیش‌فرض ۳۰ درخواست است. در صورت تجاوز، اتصال قطع می‌شود.&lrm;

&rlm;**«بر اساس هر درخواست پروکسی مجزا» به این معنی است که هر درخواست پروکسی به طور جداگانه شمارش می‌شود و بر دیگری تأثیر نمی‌گذارد**، حتی اگر همه آنها در یک اتصال H2/H3 مشترک باشند. این معنی sc (مخفف sub-connection) است. **برای کاهش fingerprint، دو پارامتر اول می‌توانند به صورت محدوده (range) تنظیم شوند**، مثلاً به ترتیب به صورت رشته‌های "500000-1000000" و "10-50" که هر بار به صورت تصادفی انتخاب می‌شوند. تمام این پارامترها می‌توانند از طریق `extra` به کلاینت ارسال شوند که در انتهای مقاله توضیح داده شده است. **علاوه بر این، شایان ذکر است که آخرین نسخه Xray، packet-up را بهینه کرده است و سرعت آن حتی به stream-up نزدیک شده است که عمدتاً به نفع QUIC H3 از طریق CDN است.**&lrm;

## &rlm;  H1 / H2 / H3&lrm;

&rlm;حال که حالت packet-up را داریم که تقریباً از تمام میدباکس‌های HTTP عبور می‌کند، بیایید در مورد یک موضوع جالب صحبت کنیم: **QUIC H3 از طریق CDN، یعنی همان عصر جدیدی که SplitHTTP آغاز کرد. درک این بخش برای استفاده انعطاف‌پذیر از XHTTP بسیار مهم است.**&lrm;

&rlm;یکی از ویژگی‌های بسیاری از میدباکس‌های HTTP این است که نسخه HTTP را تبدیل می‌کنند. مثلاً CDN یا Nginx ممکن است ترافیک H3 دریافتی را به H1 یا H2 تبدیل کرده و سپس به مبدا ارسال کنند. **یعنی سرور XHTTP ما می‌تواند فقط به H1 و H2 گوش دهد و نیازی به گوش دادن به H3 ندارد، اما کلاینت XHTTP همچنان می‌تواند از H3 استفاده کند.**&lrm;

&rlm;**این رفتار پیش‌فرض سرور XHTTP است: فقط به پورت TCP گوش داده و ترافیک H1 و H2 را پردازش می‌کند.** هنگامی که TLS فعال باشد و در `alpn` فقط یک عنصر "h3" تنظیم شده باشد، سرور فقط از quic-go برای گوش دادن به پورت UDP و پردازش ترافیک H3 استفاده می‌کند. اما در حال حاضر توصیه نمی‌شود این کار را انجام دهید، **بلکه بهتر است XHTTP را پشت یک Nginx یا Caddy واقعی پنهان کنید تا fingerprint کاهش یابد. این یکی از مزایای مهم XHTTP نسبت به سایر پروکسی‌های مبتنی بر QUIC است. ~~مزیت دیگر البته توانایی عبور از CDN است.~~** علاوه بر این، کنترل ازدحام (congestion control) در H3 در لایه application پیاده‌سازی شده است. از نظر تئوری می‌توانید الگوریتم‌های کنترل ازدحام QUIC این نرم‌افزارهای پروکسی را تغییر داده و کامپایل کنید ~~تا برخی افراد بتوانند به اصطلاح «ارسال بسته‌های خشن» (brute-force packet sending) را انجام دهند~~.&lrm;

&rlm;**برای کلاینت XHTTP:**&lrm;
&rlm;۱. **هنگام فعال بودن TLS/REALITY، به طور پیش‌فرض از H2 استفاده می‌شود**، در غیر این صورت از HTTP/1.1 استفاده می‌شود.&lrm;
&rlm;۲. هنگام فعال بودن TLS، اگر `alpn` فقط شامل "http/1.1" باشد، از HTTP/1.1 استفاده می‌شود (اما Xray اجازه نمی‌دهد که این تنظیمات fingerprint مرورگر uTLS را تغییر دهد).&lrm;
&rlm;۳. **هنگام فعال بودن TLS، اگر `alpn` فقط شامل "h3" باشد، از quic-go H3 استفاده می‌شود.**&lrm;
&rlm;۴. با این حال، هنگام استفاده از Browser Dialer، نسخه HTTP خاص توسط مرورگر تعیین می‌شود (کل تنظیمات `tlsSettings` بی‌اثر خواهند شد).&lrm;

&rlm;**اگر می‌خواهید نسخه HTTP واقعی استفاده‌شده توسط کلاینت Xray، host، mode مربوط به XHTTP، اطلاعات جداسازی uplink/downlink و غیره را تأیید کنید، کافی است سطح لاگ را روی "info" تنظیم کنید.**&lrm;

&rlm;**پروکسی QUIC H3 از طریق CDN، حداقل XHTTP اولین موردی بود که در مقیاس بزرگ پیاده‌سازی شد و راه جدیدی گشود**، زیرا در برخی مناطق یا توسط برخی ارائه‌دهندگان اینترنت، ممیزی روی H3 سخت‌گیرانه نیست ~~اگرچه برخی ممکن است آن را به شدت محدود کنند~~. حتی پس از توسعه حالت stream-up و کشف این موضوع که با اضافه کردن camouflage هدر gRPC می‌توان از Cloudflare عبور کرد، این فقط روی H2 عمل می‌کند. ~~به نظر می‌رسد ارزش این عصر جدید همچنان در حال افزایش است.~~&lrm;

## &rlm;  XMUX&lrm;

&rlm;**از آنجایی که از H2 و H3 صحبت کردیم، نمی‌توانیم از multiplexing آنها چشم‌پوشی کنیم: هر دو ۰-RTT هستند.** تفاوت در این است که H3 مشکل head-of-line blocking مربوط به TCP در H2 را ندارد و همچنین از مهاجرت اتصال (connection migration) پشتیبانی می‌کند، بنابراین تغییر شبکه توسط کلاینت باعث قطع اتصال نمی‌شود. ~~پس دوستانی که مرتباً RFCها را مطالعه می‌کنند خواهند پرسید~~: چگونه به طور خاص multiplexing آنها را کنترل کنیم؟ **ما یک رابط ساده اما قدرتمند طراحی کرده‌ایم به نام XMUX:**&lrm;

&rlm;- `maxConcurrency`: حداکثر تعداد درخواست پروکسی همزمان در هر اتصال TCP/QUIC. **پس از رسیدن تعداد درخواست‌های پروکسی در یک اتصال به این مقدار، core اتصال جدیدی ایجاد می‌کند** تا درخواست‌های پروکسی بیشتری را در خود جای دهد. **وقتی تمام مقادیر XMUX صفر باشند، مقدار پیش‌فرض این پارامتر "16-32" است که به صورت تصادفی انتخاب می‌شود.**&lrm;

&rlm;- `maxConnections`: حداکثر تعداد اتصالات همزمان. **تا قبل از رسیدن به این تعداد اتصال، هر درخواست پروکسی جدید یک اتصال جدید ایجاد می‌کند.** پس از آن، core شروع به استفاده مجدد از اتصالات موجود می‌کند. این پارامتر با `maxConcurrency` در تضاد است و فقط یکی از آنها باید انتخاب شود. مقدار پیش‌فرض ۰ است (یعنی بدون محدودیت). امکان تعیین محدوده (range) وجود دارد که به صورت تصادفی انتخاب می‌شود.&lrm;

&rlm;- `cMaxReuseTimes`: حداکثر تعداد دفعاتی که یک اتصال می‌تواند مورد استفاده مجدد قرار گیرد. **پس از استفاده مجدد به این تعداد، اتصال دیگر برای درخواست‌های پروکسی جدید تخصیص داده نمی‌شود** و پس از بسته شدن آخرین درخواست پروکسی داخلی، قطع خواهد شد. مقدار پیش‌فرض ۰ است (یعنی بدون محدودیت). امکان تعیین محدوده (range) وجود دارد که به صورت تصادفی انتخاب می‌شود.&lrm;

&rlm;- `hMaxRequestTimes`: @xqzr متوجه شد که Nginx به طور پیش‌فرض حداکثر اجازه می‌دهد که هر اتصال TCP/QUIC حداکثر ۱۰۰۰ درخواست HTTP را تجمیع کند. **وقتی تمام مقادیر XMUX صفر باشند، مقدار پیش‌فرض این پارامتر "600-900" است که به صورت تصادفی انتخاب می‌شود**، در غیر این صورت مقدار پیش‌فرض آن ۰ است (یعنی بدون محدودیت). **این پارامتر بر اساس شمارش درخواست‌های HTTP عمل می‌کند.** به طور کلی، stream-one فقط یک درخواست HTTP تولید می‌کند، stream-up دو درخواست و packet-up تعداد N درخواست تولید می‌کند. این شمارش دقیق نیست و همچنین درخواست‌های GET در Golang دارای تلاش مجدد خودکار هستند، بنابراین توصیه نمی‌شود که مقدار آن را در حداکثر ممکن تنظیم کنید. باید مقادیر مربوط به CDNها و غیره را آزمایش کرد. **در حالت packet-up، هنگامی که ارسال POSTها به صورت حلقه‌ای از این مقدار فراتر رود، به طور خودکار به یک اتصال TCP/QUIC دیگر سوئیچ می‌کند، که یک reuseTimes از آن اشغال می‌کند اما روی concurrency تأثیری ندارد.** در واقع با توجه به سه مقدار پیش‌فرض XMUX، حالت‌های stream-* از این حد فراتر نمی‌روند، فقط packet-up ممکن است از آن فراتر رود و این حالت پیش‌فرض H3 است. **بنابراین افزودن این پارامتر عمدتاً به نفع H3 است.**&lrm;

&rlm;- `hMaxReusableSecs`: @xqzr متوجه شد که Nginx به طور پیش‌فرض حداکثر اجازه می‌دهد که هر اتصال TCP/QUIC به مدت یک ساعت مورد استفاده مجدد قرار گیرد. **وقتی تمام مقادیر XMUX صفر باشند، مقدار پیش‌فرض این پارامتر "1800-3000" است که به صورت تصادفی انتخاب می‌شود**، در غیر این صورت مقدار پیش‌فرض آن ۰ است (یعنی بدون محدودیت). **پس از گذشت این مدت زمان از تداوم اتصال TCP/QUIC، اتصال دیگر برای درخواست‌های HTTP جدید تخصیص داده نمی‌شود** و پس از بسته شدن آخرین درخواست HTTP داخلی، قطع خواهد شد. در حالت packet-up، هنگامی که ارسال POSTها به صورت حلقه‌ای از این زمان فراتر رود، به طور خودکار به یک اتصال TCP/QUIC دیگر سوئیچ می‌کند، که یک reuseTimes از آن اشغال می‌کند اما روی concurrency تأثیری ندارد.&lrm;

&rlm;- `hKeepAlivePeriod`: هنگامی که اتصال H2/H3 بیکار است، کلاینت هر چند ثانیه یک بار بسته keep-alive ارسال می‌کند. مقدار پیش‌فرض ۰ است که معادل ۴۵ ثانیه برای Chrome H2 یا ۱۰ ثانیه برای quic-go H3 است. این تنها پارامتر در XMUX است که اجازه تعیین محدوده (range) را نمی‌دهد (تصادفی بودن این پارامتر خود یک ویژگی شناسایی است) و امکان تعیین مقدار منفی نیز وجود دارد (مثلاً تنظیم آن روی -۱ باعث غیرفعال شدن keep-alive در زمان بیکاری می‌شود). توصیه می‌شود آن را روی ۰ رها کنید.&lrm;

&rlm;این پارامترهای ارائه‌شده توسط XMUX می‌توانند ترکیب‌های مختلفی از کاربردها را ایجاد کنند. مثلاً قبل از speedtest چندنخی باید `"maxConcurrency": 1` تنظیم شود، و برای استفاده مجدد «نامحدود» می‌توان `"maxConnections": 1` تنظیم کرد. حتی اگر حوصله تحقیق ندارید نیز مشکلی نیست. **وقتی تمام این مقادیر صفر باشند، سه مقدار پیش‌فرض ذکر شده در بالا اعمال می‌شوند، که در عمل به معنای تعویض اتصال اصلی H2/H3 در بازه‌های زمانی است و بسیار روان عمل می‌کند.** دیگر آن تجربه «قطع جریان» (断流) ناشی از استفاده مداوم از یک اتصال مشترک در لایه‌های انتقال gRPC یا HTTP را نخواهید داشت. به طور مشابه، این پارامترها نیز می‌توانند از طریق `extra` به کلاینت ارسال شوند.&lrm;

&rlm;توجه: عدم پر کردن همه موارد نیز معادل صفر بودن همه آنهاست و سه مقدار پیش‌فرض اعمال می‌شوند، **اما اگر حتی یک مورد پر شود، سایر موارد دیگر مقدار پیش‌فرض نخواهند داشت و باید همه آنها را خودتان پر کنید**، به جز دو مورد اول، سایر موارد را می‌توان همزمان پر کرد.&lrm;

&rlm;**علاوه بر این، هنگام استفاده از XHTTP، mux.cool را فعال نکنید. همچنین نسخه جدید Xray در سمت سرور دارای بررسی است و فقط XUDP خالص را می‌پذیرد.**&lrm;

&rlm;در مورد مقادیر پیش‌فرض XMUX، برخی نقل‌قول‌ها:&lrm;

&rlm;> - من عمداً دو گزینه را انتخاب کردم که خارج از TLS به سختی قابل تشخیص هستند، یعنی maxConcurrency به همراه cMaxReuseTimes (به جای maxConnections به همراه cMaxLifetimeMs). همچنین مقادیر پیش‌فرض این دو گزینه به صورت محدوده (range) و تصادفی هستند تا بیشترین حد ممکن ویژگی‌های شناسایی بالقوه حذف شوند.&lrm;
&rlm;> - من maxConcurrency را به جای maxConnections انتخاب کردم تا از تبدیل شدن تعداد اتصالات به یک الگوی ثابت (fixed pattern) جلوگیری کنم، البته اگر شما دومی را ترجیح می‌دهید می‌توانید آن را به صورت دستی تنظیم کنید.&lrm;

&rlm;> - XMUX و Nginx اشیاء شمارش متفاوتی دارند. maxConcurrency و cMaxReuseTimes هر دو بر اساس «اتصالات پروکسی‌شده» شمارش می‌شوند. فقط stream-one یک درخواست HTTP تولید می‌کند، در حالی که stream-up یک درخواست برای uplink و یک درخواست برای downlink (یعنی دو درخواست) تولید می‌کند و packet-up تعداد N درخواست تولید می‌کند.&lrm;
&rlm;> - با این حال، مطمئن نیستم که stream-one در برخی شرایط به طور خودکار یک درخواست HTTP اضافی تولید کند یا خیر. **همچنین فکر می‌کنم دنبال کردن استفاده مجدد بی‌پایان از یک اتصال چندان معنی‌دار نیست، زیرا اگر شما ارائه‌دهنده خدمات اینترنتی باشید، شما نیز اتصالات قدیمی را محدود و پاکسازی می‌کنید، در غیر این صورت منابع به تدریج توسط اتصالات قدیمی اشغال می‌شوند. بنابراین پارامترهای پیش‌فرض XMUX محدود کردن استفاده مجدد و تعویض دوره‌ای اتصال هستند.**&lrm;

&rlm;در ادامه مقاله نیز در مورد برخی پارامترهای Nginx بحث شده است.&lrm;

## &rlm;  STREAM-UP/ONE&lrm;

&rlm;سرانجام نوبت به حالت مهم دیگر XHTTP می‌رسد: **حالت stream-up به معنای «جریان بالا‌رونده، جریان پایین‌رونده». همانطور که از نامش پیداست، uplink در این حالت نیز به صورت جریانی است و در نتیجه کارایی uplink قربانی نمی‌شود.** این حالت در ابتدا برای REALITY توسعه داده شد، تا زمانی که متوجه شدیم با اضافه کردن camouflage هدر gRPC، H2 می‌تواند از Cloudflare عبور کند (**نیاز به فعال کردن پشتیبانی gRPC در پنل دارد**) و همچنین پشتیبانی نرم‌افزارهای پروکسی معکوس مانند Nginx نیز خوب است (**توصیه می‌شود در Nginx از grpc_pass استفاده کنید، ساده و بی‌دردسر است**). بنابراین رفتار مقدار پیش‌فرض "auto" برای `mode` به شرح زیر است:&lrm;

&rlm;- **در سمت کلاینت: در صورت استفاده از TLS H2 → stream-up، در صورت استفاده از REALITY → stream-one** (در صورت وجود `downloadSettings` → stream-up)، **در غیر این صورت → packet-up**.&lrm;
&rlm;- در سمت سرور: به طور پیش‌فرض هر سه حالت را می‌پذیرد. **اگر روی یک حالت خاص تنظیم شود، فقط همان حالت را می‌پذیرد.** حالت "stream-up" یک استثنا است و stream-one را نیز می‌پذیرد.&lrm;

&rlm;> stream-up نسبت به stream-one سازگاری بهتری دارد. برخی از دوستان در گروه گفته‌اند که با فعال کردن stream-up در Cloudflare می‌توان از آن استفاده کرد، اما برای استفاده از stream-one باید یک گزینه دیگر نیز فعال شود (شاید به دلیل camouflage SSE باشد؟). همچنین دیده شده که یک CDN (نامش را فراموش کرده‌ام) هنگام استفاده از stream-one سرعت را به شدت محدود می‌کند، اما با stream-up این مشکل وجود ندارد.&lrm;

&rlm;**روش پیاده‌سازی آنها به شرح زیر است (افراد غیرتوسعه‌دهنده می‌توانند این بخش را نادیده بگیرند):**&lrm;

&rlm;- برای stream-up، کافی است packet-up uplink را به صورت جریانی `POST /yourpath/sameUUID` تغییر دهیم. همچنین `Referer: ...?x_padding=XXX...` نیز وجود دارد.&lrm;
&rlm;- برای stream-one، کافی است `POST /yourpath/` ارسال شود. پاسخ همان downlink است و به صورت دوطرفه جریانی است. هدرهای درخواست و پاسخ هر دو دارای header padding هستند.&lrm;
&rlm;- توجه داشته باشید که در stream-one اگر `/yourpath` وارد شود، درخواست واقعی به `/yourpath/` ارسال می‌شود. اگر در انتها `/` وجود نداشته باشد، به طور خودکار اضافه می‌شود.&lrm;
&rlm;- **uplinkها به طور پیش‌فرض دارای `Content-Type: application/grpc` هستند تا به عنوان gRPC camouflage شوند (می‌توان با تنظیم `noGRPCHeader` آن را غیرفعال کرد).**&lrm;
&rlm;- هدرهای پاسخ سرور برای downlink با مورد ۱ در packet-up کاملاً یکسان است. در stream-one صحنه عجیبی رخ می‌دهد که در آن یک پاسخ SSE به یک درخواست gRPC داده می‌شود. در صورت بروز مشکل می‌توان `noSSEHeader` را امتحان کرد.&lrm;

&rlm;> آزمایش‌های مرتبط در https://github.com/XTLS/Xray-core/discussions/4113#discussioncomment-11682833 نشان داد که Cloudflare اتصالات HTTP downlink را که بیش از ۱۰۰ ثانیه فاقد داده واقعی باشند، قطع می‌کند که این امر باعث قطع شدن جهت uplink در stream-up می‌شود. **بنابراین این [PR](https://github.com/XTLS/Xray-core/pull/4306) پارامتر `scStreamUpServerSecs` را به سمت سرور اضافه کرد. مقدار پیش‌فرض آن "20-80" است که به صورت تصادفی انتخاب می‌شود. سرور هر چند وقت یک بار به اندازه `xPaddingBytes` بایت ارسال می‌کند تا اتصال زنده بماند.**&lrm;
&rlm;>&lrm;
&rlm;> می‌توان `"scStreamUpServerSecs": -1` را تنظیم کرد تا این مکانیسم غیرفعال شود. در این حالت سرور حتی هدرهای پاسخ را نیز به موقع ارسال نمی‌کند و رفتار مشابه نسخه‌های قبلی خواهد بود.&lrm;

&rlm;**پس دوستانی که مرتباً از لایه انتقال gRPC استفاده می‌کنند خواهند پرسید: stream-up نسبت به لایه انتقال gRPC چه مزایایی دارد؟**&lrm;

&rlm;- مورد اول به هیچ کتابخانه gRPC‌ای نیاز ندارد و **عملکرد بهتری دارد**.&lrm;
&rlm;- ترافیک downlink در مورد اول یک درخواست GET مستقل است و **تحت تأثیر محدودیت‌های ترافیکی CDN روی gRPC قرار نمی‌گیرد**.&lrm;
&rlm;- مورد اول همچنین دارای header padding، **XMUX**، جداسازی uplink/downlink و سایر بهبودها است و مکانیسم extra نیز در آن معرفی شده است، بنابراین تمام پارامترها قابل اشتراک‌گذاری هستند و **بالغ‌تر است**.&lrm;

&rlm;البته، مزایای XHTTP نسبت به WebSocket یا HTTPUpgrade، به جز **«عدم وجود ویژگی شناسایی واضح ALPN برابر با http/1.1»**، همانطور که تا اینجا خوانده‌اید، احتمالاً پاسخ آن را خودتان می‌دانید و من نیازی به فهرست کردن آنها ندارم. ~~اصلاً هم به این دلیل نیست که تعداد آنها زیاد است و فهرست کردنشان سخت است.~~&lrm;

## &rlm;  جداسازی uplink/downlink&lrm;

&rlm;**ستاره اصلی نمایش، البته، یک عصر جدید دیگر است: جداسازی uplink/downlink.** تقریباً می‌دانیم که تشخیص دیوار آتش برای ویژگی‌های ترافیکی مانند TLS in TLS بر اساس یک اتصال واحد است. **پس اگر uplink و downlink را به سیستم‌های بازرسی مختلف تقسیم کنیم، مثلاً uplink روی IPv4 TCP و downlink روی IPv6 UDP اجرا شود، دیوار آتش نمی‌تواند به سرعت واکنش نشان دهد.** از آنجایی که سرور XHTTP تنها بر اساس UUID تصادفی تولیدشده در مسیر (path)، uplink و downlink را مرتبط می‌کند، **packet-up و stream-up ذاتاً دارای قابلیت جداسازی واقعی uplink/downlink هستند** و از آنجایی که XHTTP می‌تواند از انواع CDNها عبور کند و با REALITY و غیره ترکیب شود، گزینه‌های بی‌شماری در اختیار داریم. برای کلاینت، باید `downloadSettings` را تنظیم کنید:&lrm;

```jsonc
"xhttpSettings": {
    "host": "example.com",
    "path": "/yourpath", // must be the same
    "mode": "auto",
    "extra": {
        "headers": {
            // "key": "value"
        },
        "xPaddingBytes": "100-1000",
        "noGRPCHeader": false, // stream-up/one, client only
        "noSSEHeader": false, // server only
        "scMaxEachPostBytes": 1000000, // packet-up only
        "scMinPostsIntervalMs": 30, // packet-up, client only
        "scMaxBufferedPosts": 30, // packet-up, server only
        "scStreamUpServerSecs": "20-80", // stream-up, server only
        "xmux": { // h2/h3 mainly, client only
            "maxConcurrency": "16-32",
            "maxConnections": 0,
            "cMaxReuseTimes": 0,
            "hMaxRequestTimes": "600-900",
            "hMaxReusableSecs": "1800-3000",
            "hKeepAlivePeriod": 0
        },
        "downloadSettings": { // client only
            "address": "", // another domain/IP
            "port": 443,
            "network": "xhttp",
            "security": "tls",
            "tlsSettings": {
                ...
            },
            "xhttpSettings": {
                "path": "/yourpath", // must be the same
                ...
            },
            "sockopt": {} // will be replaced by upload's "sockopt" the latter's "penetrate" is true
        }
    }
}
```

&rlm;**همانطور که مشاهده می‌کنید، `downloadSettings` در واقع یک مجموعه کاملاً جدید از `streamSettings` است**، اما دارای `address` و `port` مشابه خروجی VLESS است تا به ورودی دیگری اشاره کند. واضح است که `network` در آن باید "xhttp" باشد (نباید حذف شود) و `security` می‌تواند "tls" یا "reality" باشد. **مورد `sockopt` نیز قابل اشتراک‌گذاری است، اما طرف دریافت‌کننده می‌تواند در `sockopt` مربوط به uplink مقدار `penetrate` را روی true تنظیم کند تا downlink را بازنویسی کند که برای مواردی مانند تنظیم `mark` مناسب است. به جز این استثنا، هنگام جداسازی uplink/downlink، پیکربندی downlink کاملاً مستقل است و هیچ تنظیماتی را از uplink به ارث نمی‌برد** و همچنین، حتی اگر XMUX در هر دو جهت پر نشده باشد و مقادیر پیش‌فرض اعمال شوند، مقادیر مشخص انتخاب‌شده به صورت تصادفی در محدوده مربوطه برای هر جهت مستقل و نامرتبط با جهت دیگر هستند. **بدین ترتیب با گذشت زمان، استفاده مجدد در uplink و downlink کاملاً نامتقارن است و زمان تعویض اتصال اصلی نیز متفاوت خواهد بود که اثر ضد تحلیل بهتری دارد.** زیرا اگر دیوار آتش بخواهد علیه جداسازی uplink/downlink اقدام کند، قطعاً «زمان شروع یکسان اتصال اصلی» مهم‌ترین نقطه شروع خواهد بود. بنابراین در آینده XHTTP همچنین اجازه خواهد داد که اتصالات uplink و downlink از ابتدا در زمان‌های مختلف آغاز شوند.&lrm;

&rlm;در واقع، اگر از CDN استفاده می‌کنید، حتی بدون تغییر پیکربندی سرور نیز می‌توانید جداسازی uplink/downlink را اجرا کنید. **مثلاً می‌توانید یک IPv4 بهینه برای اجرای TLS H2 و یک IPv6 بهینه برای اجرای QUIC H3 انتخاب کنید.** همچنین اکثر CDNها از «دامنه‌های جلویی یکسان» (same-domain fronting) پشتیبانی می‌کنند. مثلاً در uplink مقدار `serverName` را "a.example.com" و در downlink مقدار `serverName` را "b.example.com" قرار دهید، در حالی که مقدار `host` در هر دو جهت "c.example.com" باشد. **بدین ترتیب SNI خارجی نیز متفاوت به نظر می‌رسد.** البته اگر از قبل دو دامنه مجزا دارید، حتی بهتر است. اگر هیچ دامنه‌ای ندارید، می‌توانید دو سرور مجازی (VPS) راه‌اندازی کنید و دو REALITY جداگانه برای جداسازی uplink/downlink اجرا کنید: **چه با CDN به همراه پروکسی معکوس و چه با REALITY به همراه fallback، تا زمانی که در نهایت با مسیر (path) یکسان به یک ورودی XHTTP روی یک سرور مجازی مشترک برسند، کافی است.** در کل، از آنجایی که XHTTP همه‌جا قابل استفاده است، ترکیب‌های بی‌شماری را می‌توان ایجاد کرد و تنها محدودیت، اندازه خلاقیت شماست.&lrm;

&rlm;**مثلاً پس از معرفی جداسازی uplink/downlink، بسیاری از افراد در واقع uplink را به خطوطی با رفت بهینه و downlink را به خطوطی با برگشت بهینه اختصاص می‌دهند که این نیز کاربردی است.**&lrm;

&rlm;مثلاً می‌توانید برای uplink مقدار `"maxConnections": 1` و برای downlink مقدار `"maxConcurrency": 1` تنظیم کنید. **بدین ترتیب داده‌های کم‌حجم uplink همه از یک اتصال زیرین مشترک عبور می‌کنند، در حالی که داده‌های پرحجم downlink روی اتصالات زیرین مختلف توزیع می‌شوند که همزمان دارای اثر ضد سانسور، تاخیر کم و سرعت بالا است.** کمی شبیه به سوئیچ است و در ترکیب با Vision Seed نتیجه بهتری دارد.&lrm;

&rlm;در بالا نمونه‌ای از پیکربندی شامل تمام پارامترها ارائه شد. **هدف اصلی این است که بدانید هر مورد را کجا باید بنویسید** و همچنین پارامترهایی که در متن قبلی توضیح داده نشده‌اند را شرح دهیم:&lrm;

&rlm;- **`extra` طرح اشتراک‌گذاری JSON خام برای تمام پارامترهای به جز `host`، `path` و `mode` است. هنگامی که `extra` وجود دارد، فقط این چهار مورد اعمال می‌شوند. همچنین در لینک‌های اشتراک‌گذاری فقط این چهار مورد وجود دارند و معمولاً در رابط کاربری گرافیکی (GUI) نیز فقط این چهار مورد نمایش داده می‌شوند، زیرا پارامترهای داخل `extra` نسبتاً کم‌کاربرد هستند و باید مستقیماً توسط ارائه‌دهنده سرویس به کلاینت ارسال شوند و نباید اجازه داد که کلاینت به طور دلخواه آنها را تغییر دهد.**&lrm;
&rlm;توضیح تکمیلی: منظور از «ارسال» این است که «شخص آنها را ارسال کند»، یعنی ارائه‌دهنده سرویس پس از نوشتن `extra`، کل آن را در لینک اشتراک‌گذاری قرار دهد و کلاینت می‌تواند مستقیماً آن را به عنوان `extra` خود استفاده کند.&lrm;

&rlm;- رفتار `host` مشابه سایر لایه‌های انتقال مبتنی بر HTTP در Xray است. **اولویت ارسال host توسط کلاینت به ترتیب `host` > `serverName` > `address` است.** اگر در سرور `host` تنظیم شده باشد، مقدار ارسالی از سمت کلاینت بررسی می‌شود تا مطابقت داشته باشد، در غیر این صورت بررسی انجام نمی‌شود. توصیه می‌شود بدون دلیل خاصی آن را تنظیم نکنید. `host` را نباید داخل `headers` قرار داد.&lrm;

&rlm;> **اگر می‌خواهید نسخه HTTP واقعی استفاده‌شده توسط کلاینت Xray، host، mode مربوط به XHTTP، اطلاعات جداسازی uplink/downlink و غیره را تأیید کنید، کافی است سطح لاگ را روی "info" تنظیم کنید.**&lrm;

## &rlm;  فراتر از REALITY&lrm;

&rlm;**اوایل سال گذشته من بازگشتم و REALITY را نوشتم که ~~همه چیز را تحت الشعاع قرار داد~~ و چندین نقطه درد را یکجا حل کرد.** ~~سپس تقریباً هر روز به کلاب می‌رفتم و زیاد به آن رسیدگی نمی‌کردم~~، حتی مقاله آن نیز تاکنون منتشر نشده است. ~~مقاله XUDP UoT Migration نیز متأسفانه سال گذشته تقریباً کامل شده بود اما منتشر نشد.~~ **ناگهان متوجه شدیم که REALITY به طور پنهانی به ستون اصلی اتصال مستقیم تبدیل شده است.** اغلب می‌بینیم که وقتی فردی گزارش می‌دهد XX مسدود شده است، نظرات پیشنهاد می‌کنند که به REALITY تغییر دهد. اعتبار آن برای همه آشکار است. ~~حتی به دلیل پایداری بیش از حد، اینجا دیگر مانند گذشته شلوغ نیست.~~ اگرچه Xray نیز بهینه‌سازی‌ها و بهبودهای زیادی برای سایر transports انجام داده است، **اما XHTTP اولین لایه انتقال بومی Xray است. ~~اولین موردش یک حرکت بزرگ بود.~~** اکنون که این مقاله را خوانده‌اید، همچنین می‌دانید که XHTTP می‌تواند همراه با REALITY استفاده شود:&lrm;

&rlm;**منظور از Beyond REALITY جایگزینی REALITY نیست، بلکه محبوبیت فراتر از REALITY است. بدون اغراق می‌توان گفت، همانطور که همیشه سبک Xray بوده است، ظهور XHTTP باعث شده است که تمام لایه‌های انتقال مبتنی بر HTTP دیگر رنگ ببازند. این عصر همه‌کاره بودن XHTTP است. XHTTP از آخرین پروتکل موفق، یعنی REALITY، محبوب‌تر خواهد بود، زیرا ویژگی‌های XHTTP از قبل تعیین کرده‌اند که دامنه تحت پوشش آن گسترده‌تر خواهد بود.**&lrm;

&rlm;در پایان، امیدوارم ظهور XHTTP بتواند شگفتی کوچکی برای شما ایجاد کند، همانطور که Xray بارها در تاریخ خود انجام داده است: **انقلاب و نوآوری، همیشه اعتقاد Xray بوده است.**&lrm;

## &rlm; # اگر این مقاله برای شما مفید بود، از یک [REALITY NFT](https://opensea.io/assets/ethereum/0x5ee362866001613093361eb8569d59c4141b76d1/2) حمایت کنید:&lrm;

&rlm;- تعداد کل هزار عدد است. قیمت صد عدد اول ۰.۰۱ اتر است و پس از آن قیمت افزایش می‌یابد. **دریافت نسخه‌های اولیه به معنای سود خالص است.**&lrm;
&rlm;- ~~مقاله‌ای در معرفی REALITY در روزهای آینده منتشر خواهد شد~~ که تبدیل به معرفی XHTTP شد.&lrm;
&rlm;- **دارندگان REALITY NFT واجد شرایط دسترسی زودهنگام به یک کلاینت جدید Xray و همچنین ایردراپ NFT بعدی خواهند بود، مشابه**&lrm;
&rlm;- دارندگان Project X NFT در تاریخ ۲۰۲۵.۱.۱ دو عدد REALITY NFT هدیه دریافت خواهند کرد.&lrm;

&rlm;با حمایت شما، Project X NFT پنج تا شش برابر افزایش قیمت داشته است. برای دوستانی که در زمان عرضه اولیه Project X NFT مردد بودند، این فرصت خوبی برای جبران است.&lrm;

## &rlm; # البته اگر توانایی دارید، حمایت از [Project X NFT](https://github.com/XTLS/Xray-core/discussions/3633) حتی سپاسگزارتر خواهد بود:&lrm;

&rlm;- ETH/USDT/USDC: `0xDc3Fe44F0f25D13CACb1C4896CD0D321df3146Ee`&lrm;
&rlm;- Project X NFT: [Announcement of NFTs by Project X #3633](https://github.com/XTLS/Xray-core/discussions/3633)&lrm;
&rlm;- REALITY NFT: https://opensea.io/assets/ethereum/0x5ee362866001613093361eb8569d59c4141b76d1/2&lrm;
&rlm;- گاهی هزینه gas اتر به طور غیرمنطقی بالا می‌رود. می‌توانید ابتدا آن را به علاقه‌مندی‌های خود اضافه کنید و زمانی که هزینه gas نسبتاً عادی شد، خریداری کنید.&lrm;

&rlm;اگر هنوز نقطه مبهمی وجود دارد، لطفاً در بخش نظرات بیان کنید تا مقاله را به روز کنم. ~~توصیه می‌شود نسخه ترجمه شده را خیلی زود منتشر نکنید.~~&lrm;
